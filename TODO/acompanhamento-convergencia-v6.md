# Acompanhamento de convergência — auditoria pré-release v6.0.0

Rastreia se os rounds de auditoria do v6 estão convergindo (menos achados, sem
regressão) ou divergindo. Atualizado uma vez por round completo, não por fix
individual. Ver `[[project_convergence_tracking_v5]]` (memória) para o raciocínio
original por trás de separar este documento dos outros dois — mesma lógica aplicada
aqui para v6, série própria por não compartilhar achados com a auditoria v5.1.

**Critério de convergência:** 2 rounds consecutivos com zero Crítico/Alto aberto, sem
regressões, e total de achados abertos não crescendo.

| Round | Data       | Crítico | Alto | Médio | Baixo | Observações |
|-------|------------|---------|------|-------|-------|--------------|
| 1     | 2026-08-23 | 1       | 4    | 8     | 5     | Round-base (primeira rodada da série v6, sem round anterior para comparar convergência). 3 dos 18 achados eram "candidatos" sinalizados pelo próprio agente como pendentes de corroboração — todos os 3 foram corroborados nesta mesma rodada (1 confirmado 2/2 igual severidade — pin; 1 desempatado 2/3 com reclassificação de severidade — fan-out de Tasks, Médio→Baixa). 1 achado corroborado independentemente por 2 frentes distintas (link morto `usage-background-logger.md`, usabilidade+observabilidade). 5/6 frentes precisaram de retentativa por falha de infraestrutura (stall/erro de API), não de conteúdo. |
| 1 (fechamento) | 2026-08-24 | 0 aberto | 0 aberto | 0 aberto | 0 aberto | **Todos os 18 achados fechados nesta mesma rodada** — 15 corrigidos (doc + código, todos com red/green quando aplicável a comportamento), 1 aceito como comportamento conhecido por decisão explícita do usuário (pin parcial sob scale-down manual, mitigado com `LogWarning`), 2 descartados por decisão do usuário (H1/H2 de complexidade — refactor sem evidência empírica de valor nos defaults). Nenhum achado ficou em aberto sem decisão. Verificado: 183/183 testes net10.0, build limpo em toda a solução (3 TFMs, samples, benchmarks, gerador de docs) a cada etapa. Não convergência ainda aplicável (critério exige 2 rounds consecutivos) — próxima rodada de auditoria decidirá se este round realmente fechou tudo ou se surgem novos achados. |
| 2     | 2026-08-24 | 0       | 2    | 6     | 4     | Objetivo: caçar regressão dos fixes do Round 1 + achados novos. **Não convergiu** — 2 Alto novos encontrados: 1 introduzido pela própria correção desta sessão (guard `IsEnabled` desprotegido, violação do invariante F23), 1 pré-existente desde ADR007V03 nunca antes pego (`AddHostedService` deduplicando por tipo, só o 1º buffer de cada T recebia warmup automático). Todos os 12 achados (2 Alto + 6 Médio + 4 Baixo) fechados na mesma rodada — 11 corrigidos com red/green, 1 resolvido via mudança de doc + alternativa correta proposta (`[FromKeyedServices]`) por decisão do usuário. Nenhum achado ficou aberto sem decisão. 3 achados corroborados independentemente entre frentes distintas (overclaim do log do Monitor: usabilidade+observabilidade; fix do guard IsEnabled: complexidade descobriu o bug de comentário, estabilidade achou a violação F23 no próprio fix, desempenho confirmou com número). 3/6 frentes precisaram de retentativa por falha de infraestrutura. Verificado: 186/186 testes net10.0, build limpo (0 warnings) em toda a solução a cada etapa. |
| 2 (fechamento) | 2026-08-24 | 0 aberto | 0 aberto | 0 aberto | 0 aberto | Todas as decisões do Round 2 tomadas na mesma sessão — ver linha acima e `relatorio-auditoria-v6.md`. Ainda sem 2 rounds consecutivos "limpos" (Round 1 achou 1 Crítico/4 Alto; Round 2 achou 2 Alto novos) — critério de convergência formal ainda não atingido, mas a tendência (achados cada vez mais concentrados em correções pontuais, nenhum achado estrutural novo no Round 2 além dos 2 Alto) é de convergência. Round 3 decidirá. |
| 3     | 2026-08-24 | 0       | 1    | 2    | 4     | Objetivo: caçar regressão do Round 2 + achados novos. **Achado mais grave: o fix `SafeIsEnabled` do Round 2 nunca foi conectado** em `RingBufferManager.cs` (helper criado, call site não trocado) — confirmado de forma **independente por 3 das 6 frentes** (estabilidade, resiliência, observabilidade), cada uma reproduzindo empiricamente o mesmo cenário. É a segunda vez seguida que um fix de uma rodada anterior é encontrado incompleto/quebrado pela rodada seguinte (Round 2 achou o F23 introduzido pelo próprio Round 2; Round 3 achou que o fix desse mesmo F23 não foi de fato aplicado). Também: pendência de desempenho do CTS/timer fechada com número real (69% da alocação do caminho rápido de Acquire); 4 achados de doc da usabilidade. |
| 3 (fechamento) | 2026-08-24 | 0 aberto | 0 aberto | 0 aberto | 0 aberto | **Todos os 7 achados decididos/corrigidos** na mesma sessão (o último, assimetria de formato entre `RingBufferBuilder.LogError`/`RingBufferManager.LogError`, decidido e corrigido após confirmação do usuário). Nenhum achado ficou aberto sem decisão. Ainda sem 2 rounds "limpos" consecutivos, mas a tendência de severidade está caindo (Round 1: 1 Crítico/4 Alto; Round 2: 2 Alto; Round 3: 1 Alto) e o volume de achados por rodada também (18 → 12 → 7). Round 4 decidirá se converge de fato — recomenda-se prestar atenção redobrada a fixes "confirmados" de rodadas anteriores, dado o padrão de 2 rodadas seguidas com fix incompleto não pego pela própria verificação (testes verdes + build limpo não bastam para pegar "helper criado mas não conectado"). Verificado: 187/187 testes net10.0, build limpo (0 warnings) em toda a solução a cada etapa. |
| 4     | 2026-08-24 | 0       | 2    | 0     | 3     | Objetivo: caçar regressão do Round 3 + achados novos, com atenção redobrada a fixes "confirmados". **Nenhuma repetição do padrão das rodadas anteriores** — os 4 fixes do Round 3 (`SafeIsEnabled`, Stopwatch/delegate, comentário CTS/timer, texto do `LogError`) foram confirmados de fato conectados por pelo menos 2 frentes independentes cada, um deles (`SafeIsEnabled`) via vermelho→verde real feito por 2 frentes distintas. 2 achados Alto novos, ambos de doc (não de comportamento): `README.txt` ainda descrevendo v5.0.0/API removida (miss do próprio Round 3, que tinha marcado esse arquivo como "sem achado"); `ADR007V03`/guia de observability nunca atualizados para bater com a semântica `Logger`/`OnError` já decidida no Round 1. 2 Médio de comportamento (TOCTOU de identidade de exceção sob shutdown concorrente; assimetria de tags em `acquire.duration`), ambos com red/green. 1 Baixo de complexidade não medido virou fix de baixo risco após medição própria (boxing de bool cacheado, líquido abaixo do Round 3 apesar de 2 tags novas). 3 achados Baixo restantes: 1 documentado como limitação conhecida (janela construtor/gauge), 1 nuance textual sem ação, 2 hipóteses de complexidade descartadas/sem valor. |
| 4 (fechamento) | 2026-08-24 | 0 aberto | 0 aberto | 0 aberto | 0 aberto | **Todos os 10 achados decididos/corrigidos/documentados** na mesma sessão. Nenhum achado ficou aberto sem decisão. Critério formal de convergência (2 rounds consecutivos com zero Crítico/Alto) **ainda não atingido** — este é o 2º round consecutivo com Alto aberto durante a rodada (Round 3 teve 1 Alto), mas ambos os Alto deste round são gaps de doc não propagada, não bugs de comportamento novos, e nenhuma classe de bug nova de comportamento apareceu (os 2 Médio são refinamentos de casos-extremos já mapeados, não achados estruturais). Volume total de achados subiu levemente frente ao Round 3 (7→10), mas a composição mudou: mais achados de baixo custo/baixo risco (cache de alocação, doc), menos achados de correção de comportamento genuinamente nova. Verificado: 188/188 testes net10.0 (era 187, +1 novo), build limpo (0 warnings, 0 errors) em toda a solução a cada etapa. Recomenda-se Round 5 focado especificamente em: (a) confirmar que os 2 Alto deste round realmente pararam de reaparecer (terceira vez seria padrão, não acaso); (b) revisitar o candidato H-B/H-C de complexidade só se houver motivo novo. |
| 5     | 2026-08-24 | 0       | 2    | 2     | 0     | Objetivo: caçar regressão do Round 4 + achados novos, com foco especial em confirmar que os 2 Alto do Round 4 não reaparecem. **Resposta: não reaparecem — mas um 3º Alto da MESMA CLASSE (doc citando ADR superseded) apareceu em outro arquivo** (`CONTRIBUTING.md`, `ADR004V02`/`ADR006V01` em vez de V03/V02 — 3ª recorrência dessa classe específica no mesmo arquivo, já fechada uma vez na auditoria v5.1 para uma versão de ADR anterior). Estabilidade e resiliência — as 2 frentes que mais escrutinaram o fix `LifetimeToken()` do Round 4 — não encontraram nada novo, a primeira vez neste ciclo de rounds que 2 das 6 frentes reportam "nada novo" na mesma rodada (resiliência inclusive tentou >26.000 corridas sem pousar na janela real, corroborando o tamanho da janela em vez de encontrar um bug). 1 Médio de doc (mesma causa raiz do Alto), 1 Médio de código com red/green (Activity `"RingBufferPlus.Acquire"` tinha a mesma assimetria de tag-set que foi o achado principal do Round 4, só que no lado do trace em vez da métrica — passou 4 rounds sem ser visto). 1 achado da própria frente de desempenho sobre o PRÓPRIO Round 4: os números re-medidos de `usage-observability.md` já estavam stale (medidos antes do último fix de alocação daquele round). 1 candidato de complexidade (H4, backlog signaling sem coalescência) encaminhado, não medido. |
| 5 (fechamento) | 2026-08-24 | 0 aberto | 0 aberto | 0 aberto | 0 aberto | **Todos os 4 achados de comportamento/doc decididos/corrigidos** na mesma sessão; H4 fica explicitamente em aberto como candidato não medido para rodada futura, não como achado pendente sem decisão. Critério formal de convergência ainda não atingido (3º round consecutivo com Alto), mas a composição continua sem nenhum bug de comportamento genuinamente novo desde o Round 3 — os Alto/Médio dos Rounds 4 e 5 são todos: doc não propagada após uma decisão já tomada, ou uma classe de bug já corrigida uma vez no mesmo round reaparecendo num sinal irmão (métrica vs. trace) que o fix original não cobriu. 2 das 6 frentes (estabilidade, resiliência) convergiram nesta rodada — recomenda-se Round 6 focado em: (a) confirmar que a Activity/tag-asymmetry fix não deixou um 3º sinal irmão descoberto (ex.: seria irônico se `scale.*` também tivesse um caso residual não coberto pela varredura já feita); (b) tratar H4 com medição real antes de decidir; (c) se estabilidade/resiliência convergirem de novo, considerar reduzir esses 2 ângulos a uma checagem leve em vez de frente completa, já que 2 rounds seguidos sem achado é o próprio critério de convergência por frente. Verificado: 188/188 testes net10.0, build limpo (0 warnings, 0 errors) em toda a solução a cada etapa. |
| 6     | 2026-08-24 | 0       | 1    | 1     | 3     | Objetivo: confirmar fixes do Round 5 + achar o que passou batido em 5 rodadas, com estabilidade/resiliência em enquadramento reduzido (1ª vez nesta série). **Confirma-se exatamente a previsão (a) do Round 5**: `scale.*` tinha o mesmo caso residual de assimetria métrica/trace que `acquire.*` já teve corrigido 2x antes — Activity `"RingBufferPlus.Scale"` nunca ganhava a tag `success` (O9, Médio) — 3ª instância da mesma classe. 1 Alto de doc (O10: `usage-observability.md` afirmava falsamente que Scale correlaciona com o trace do chamador — arquiteturalmente impossível, Scale roda no loop do engine, não no fluxo do chamador). Estabilidade levantou uma suspeita de leak de `Activity.Current` entre Scale ops consecutivas, mas **investigação empírica no `RingBufferManager` real refutou a hipótese** (a fronteira de método async em `ProcessCommandAsync` reverte a mutação antes que ela escape do loop do engine) — 3ª vez que esse tipo específico de suspeita (parent-chaining de Activity) é levantada e refutada nesta série (Rounds 1, 3, 6), agora registrado explicitamente para não gastar uma 4ª passada. Resiliência: nada novo. **2ª rodada consecutiva sem achado para estabilidade e resiliência — critério formal de convergência por frente atingido para as duas** (a hipótese refutada de estabilidade não conta como achado confirmado). H4 fechado por 2 argumentos estáticos independentes (complexidade + desempenho), sem necessidade de benchmark. Desempenho pegou a própria doc do Round 5 stale de novo (1048 B medido antes do fix daquele round ter sido aplicado — 2ª vez seguida do mesmo erro de processo) — corrigido desta vez com sequenciamento explícito (todo o código primeiro, rebuild limpo, uma única medição real depois): número real 1088 B (não 1048 B) com listener, 472 B sem (estável). Usabilidade achou `v6-design-proposal.md` nunca aberto em 5 rodadas apesar de referenciado por um ADR Aceito — banner SUPERSEDED adicionado. Achado extra fora do escopo das 6 frentes: a limpeza de descontinuação da v5.1.0 (decisão nova do usuário no meio da rodada) deixou `ADR004V03` (Aceito, não histórico) com uma decisão que nunca chegou a ser publicada — corrigido no próprio ADR com a concordância explícita do usuário de que isso não fere a imutabilidade de ADR (decisão nunca executada), e uma citação órfã em `ADR003V03` para conteúdo apagado do CHANGELOG. |
| 6 (fechamento) | 2026-08-24 | 0 aberto | 0 aberto | 0 aberto | 0 aberto | **Todos os 6 achados de comportamento/doc decididos/corrigidos** na mesma sessão, mais os 2 achados de ADR fora do escopo original. Nenhum achado ficou aberto sem decisão. **Critério formal de convergência atingido pela primeira vez nesta série para 2 das 6 frentes** (estabilidade, resiliência — 2 rounds consecutivos, Round 5 e Round 6, sem achado confirmado); as outras 4 frentes continuam achando itens de baixo risco (assimetria de tag residual, doc não aberta em 5 rounds, doc stale), nenhum bug de comportamento genuinamente novo desde o Round 3. Recomenda-se Round 7 focado em: (a) considerar reduzir estabilidade/resiliência a uma checagem leve permanente (2 rounds limpos é o próprio critério que este documento define para isso); (b) confirmar que a nova tag `success` em `scale.*` não deixou nenhum outro sinal irmão descoberto (varredura já feita nesta rodada não achou nenhuma outra ocorrência, mas vale reconfirmar); (c) verificar se a disciplina de "medir uma vez, no final, depois de todo o código pronto" adotada nesta rodada realmente evita uma 3ª rodada seguida de número stale. Verificado: 190/190 testes net10.0 (era 188, +2 novos), build limpo (0 warnings, 0 errors) em toda a solução a cada etapa. |
| 7     | 2026-08-24 | 0       | 1    | 0     | 4     | Objetivo: confirmar fixes do Round 6 + achar o que passou batido em 6 rodadas. **1ª rodada desta série em que estabilidade/resiliência não são disparadas de forma alguma** (não apenas enquadramento reduzido como no Round 6) — critério de convergência por frente já as havia liberado. Achado principal: **1 Alto de comportamento genuinamente novo, não doc** — falha de warmup cacheada (`Lazy<Task>`, ADR011) deixava toda chamada implícita subsequente de `AcquireAsync`/`SwitchToAsync` sem qualquer telemetria (nem span, nem métrica, nem log), mesmo com 100% das chamadas falhando de verdade. Rompe a sequência de 2 rounds (4, 6) em que os Alto eram só doc não propagada. 1 Baixo de comportamento (heartbeat inflava `_waitingCount` mesmo devendo ser exempto, per o próprio comentário do código) e 4 Baixo de doc (3 citações de round interno vazando para guias públicos, resíduo no `ADR004V03`). 1 candidato de complexidade roteado para estabilidade (uso de `ConcurrentBag` sem afinidade de thread real) — deferido para o Round 8 por decisão do usuário, já que estabilidade não rodou nesta rodada. Confirmado via `git blame` que comentários "Round 7, Resiliência/Estabilidade" já existentes no código são de uma série de auditoria não relacionada (2026-08-21, pré-v6) — coincidência de numeração, não vazamento real desta rodada. |
| 7 (fechamento) | 2026-08-24 | 0 aberto | 0 aberto | 0 aberto | 0 aberto | **Todos os 6 achados de comportamento/doc decididos/corrigidos** na mesma sessão (as 2 decisões de comportamento levadas ao usuário com opções explícitas antes de implementar - 1A e 2B escolhidas). H-D (candidato de complexidade) fica explicitamente deferido para o Round 8, não como achado pendente sem decisão. O Alto desta rodada quebra a tendência dos Rounds 4/6 (Alto = doc não propagada) - é a primeira vez desde o Round 3 que um Alto é um bug de comportamento genuíno, não um residual de doc. Isso por si só argumenta contra reduzir ainda mais o escopo das 4 frentes restantes no Round 8: usabilidade e observabilidade continuam achando itens reais a cada rodada (nenhuma delas teve ainda uma rodada limpa nesta série). Verificado: 192/192 testes net10.0 (era 190, +2 novos), build limpo (0 warnings, 0 errors) em toda a solução a cada etapa. |
| 8     | 2026-08-24 | 0       | 2    | 1     | 1     | Objetivo: confirmar fixes do Round 7 + achar o que passou batido em 7 rodadas. Estabilidade disparada em escopo pontual (só o candidato H-D) — não retomou enquadramento completo. **2 Alto, ambos apontando para a mesma lacuna do próprio fix do Round 7**: (1) o relatório do Round 7 afirmou incorretamente que `SwitchToAsync` tinha recebido o fix de telemetria — só `AcquireCoreAsync` foi tocado; decisão: documentar como limitação conhecida, não estender o código; (2) a linha nova de falha de warmup cacheada era indistinguível, em tags, de um shutdown comum — corrigido com a 4ª tag `acquire.warmup_failed`/`warmup_failed`. 1 Médio **investigado e REFUTADO** (suspeita de log ausente em `SwitchToAsync` manual desbloqueado - CreateItemsAsync já loga por tentativa, independente de quem espera o resultado; 1ª sonda deu falso positivo por usar um factory síncrono). 1 Baixo de desempenho (custo extra de +20-24ns do próprio fix do Round 7, medido com A/B contra o commit anterior) — aceito como está por decisão do usuário. Candidato H-D (`ConcurrentBag`) liberado por estabilidade e implementado. **Achado de processo**: pela 2ª vez nesta série (a 1ª foi o benchmark do Round 4→5→6), uma auditoria anterior (Round 7) fez uma afirmação sobre o próprio escopo do fix ("AcquireAsync/SwitchToAsync") que não foi verificada contra o commit real - só corrigida quando outra frente (observabilidade, Round 8) testou empiricamente. |
| 8 (fechamento) | 2026-08-24 | 0 aberto | 0 aberto | 0 aberto | 0 aberto | **Todos os 4 achados de comportamento decididos/corrigidos/refutados** na mesma sessão (3 decisões levadas ao usuário com opções explícitas: tag nova para o Alto de tags, aceitar o custo do Baixo de desempenho, implementar a troca do ConcurrentBag agora). Nenhum achado ficou aberto sem decisão. Nenhuma das 4 frentes completas (usabilidade, complexidade, desempenho, observabilidade) bateu rodada limpa ainda nesta série - a composição dos achados continua sem nenhuma classe de bug genuinamente nova além de refinamentos do que as rodadas anteriores já mapearam (a maior parte do Round 8 foi, na prática, fechar lacunas residuais do próprio Round 7). Verificado: 193/193 testes net10.0 (era 192, +1 novo), build limpo (0 warnings, 0 errors) em toda a solução a cada etapa. |
| 9     | 2026-08-24 | 0       | 0    | 1     | 0     | Objetivo: confirmar fixes do Round 8 + achar o que passou batido em 8 rodadas, com enquadramento ajustado por pilar após análise de tendência pedida pelo usuário (complexidade reduzida; desempenho redefinido como verificação de regressão ligada a mudanças de código, não frente que converge - mudança registrada também no repositório compartilhado de definições de subagente `C:\Sources\EA4AI`; observabilidade em formato de checklist único). **Complexidade e usabilidade bateram rodada limpa pela primeira vez na série** - complexidade pela 1ª vez genuinamente (as 3 anteriores tinham candidato roteado/auto-descartado); usabilidade pela 1ª vez em 9 rounds (sempre teve pelo menos 1 Baixo antes). Desempenho mediu as 2 mudanças do Round 8 ainda não medidas: a troca `ConcurrentBag`→`lock`+`List` é ganho puro (~3-5x mais rápido, 0B vs. 32-176B), sem trade-off a decidir; a tag `acquire.warmup_failed` custa +40B/+39ns só no caminho com listener anexado, inalterado no caminho comum sem listener - não recomendado para escalar. Observabilidade, no formato checklist, achou 1 Médio genuíno (veredito "não saudável" do HeartBeat sem sinal próprio, confirmado por reprodução empírica) e entregou uma tabela de cobertura de 26 caminhos de execução - primeira vez que essa frente produz uma prova de cobertura completa, não só uma lista de achados. **0 Alto pela primeira vez desde o Round 3.** |
| 9 (fechamento) | 2026-08-24 | 0 aberto | 0 aberto | 0 aberto | 0 aberto | **O único achado (Médio, observabilidade) decidido e corrigido** na mesma sessão (4 opções apresentadas, C escolhida - contador + log). Correção começou antes do resultado de desempenho voltar, por não haver overlap de área de código - confirmado com o usuário antes de prosseguir. Nenhum achado ficou aberto sem decisão. **2 das 4 frentes ativas convergindo de fato** (complexidade e usabilidade, ambas em sua 1ª rodada limpa) - se baterem uma 2ª rodada limpa no Round 10, atingem o critério formal deste documento. Desempenho não teve nenhuma regressão para decidir - seu papel permanente de verificação, não de convergência, se confirmou na prática (mediu, achou 1 melhoria e 1 custo pequeno, nenhum dos dois precisou de decisão de trade-off). Observabilidade é a única frente ainda sem nenhum sinal de desaceleração - recomenda-se que a próxima rodada dela reavalie se a tabela de cobertura do Round 9 é suficiente para considerá-la coberta por ora, em vez de repetir o formato checklist indefinidamente. Verificado: 194/194 testes net10.0 (era 193, +1 novo), build limpo (0 warnings, 0 errors) em toda a solução a cada etapa. |
| 10    | 2026-08-24 | 0       | 1    | 0     | 2     | Objetivo: confirmar fixes do Round 9 + achar o que passou batido em 9 rodadas, mesmo enquadramento do Round 9. **Complexidade bateu a 2ª rodada limpa consecutiva — critério formal de convergência atingido pela primeira vez nesta série para um pilar completo** (não reduzido/pontual como estabilidade/resiliência antes). Desempenho, pela 1ª vez desde sua redefinição, avaliou uma mudança e concluiu explicitamente que não havia alvo plausível de medição, sem forçar um benchmark — confirma que o novo papel funciona na prática, não só na intenção. Usabilidade NÃO bateu 2ª rodada limpa: achou que a doc do próprio fix do Round 9 estava errada (dizia "único sinal" quando na verdade 2 sinais - contador e log - foram adicionados) - reinicia a contagem dessa frente em 1. Observabilidade, de volta ao formato incremental, achou 1 Alto real (`EvaluateFloorGuard` sem latch, repetindo indefinidamente e ausente da doc de Logging) e 1 Baixo (doc errada sobre quando a janela do Monitor limpa). O Alto foi resolvido com uma função pura nova testada deterministicamente (6 testes), após uma 1ª tentativa de teste de integração baseado em timing real ter se mostrado pouco confiável (o backoff de retry já auto-limita a cadência a algo parecido com a janela de graça, mascarando a diferença antigo/novo num teste cronometrado). |
| 10 (fechamento) | 2026-08-24 | 0 aberto | 0 aberto | 0 aberto | 0 aberto | **Todos os 3 achados decididos/corrigidos** na mesma sessão (1 decisão de comportamento levada ao usuário com opções explícitas - A escolhida, com a ressalva de manter o alerta repetindo como fallback). Nenhum achado ficou aberto sem decisão. **1 pilar (complexidade) atinge convergência formal completa pela primeira vez nesta série** - critério de 2 rounds consecutivos sem achado, sem enquadramento reduzido/pontual envolvido na decisão. Usabilidade e observabilidade seguem sem 2 rodadas limpas consecutivas, mas por um padrão notável: os 2 achados desta rodada nessas frentes vieram de gaps no próprio fix do Round 9 (documentação incompleta de uma decisão "C: os dois" e um efeito colateral não previsto do fix anterior) - a mesma dinâmica de auto-alimentação que já apareceu entre rounds de observabilidade (6→7→8) agora atravessando também usabilidade. Verificado: 201/201 testes net10.0 (era 194, +7 novos), build limpo (0 warnings, 0 errors) em toda a solução a cada etapa. |
| 11    | 2026-08-24 | 0       | 1    | 1     | 0     | Objetivo: confirmar fixes do Round 10 + achar o que passou batido em 10 rodadas. Complexidade fora da rotação (convergiu no Round 10) - mesmo tratamento de estabilidade/resiliência. Desempenho, pela 2ª vez, produziu uma resposta "sem medição" - desta vez com prova dedutiva (o novo predicado do Round 10 é um estreitamento estrito do antigo, logicamente não pode regredir), não só "sem alvo plausível". Usabilidade não bateu 2ª rodada limpa (reinicia em 1 pela 2ª vez seguida): achou que `concepts.md`/`usage-fixed-capacity.md` afirmavam incorretamente que `FixedCapacity` nunca escala, contradito pelo próprio floor guard e por um teste do Round 10. Observabilidade achou 1 Alto real: a caracterização de "active" do Monitor como "caso estreito" (só MaxCapacity pinado com backlog) provou ser falsa - `active` é verdadeiro também em utilização plena comum sem espera, confirmado por reprodução empírica pedida explicitamente pelo usuário antes de decidir a correção. **Decisão de processo notável**: o usuário pediu uma avaliação detalhada do custo/impacto de uma correção comportamental (opção B, estreitar `active`) antes de decidir, e a avaliação revelou que a ferramenta de simulação existente não modela a distinção necessária - decisão final foi doc-only (opção A), com B registrada como questão de design deliberadamente adiada, não como achado pendente. Investigação paralela (fora do formato de round) removeu 1 arquivo de teste redundante (`RingBufferManagerTests.cs`, 7 testes superados por `RingBufferContractTests.cs`) - nenhum resíduo de v4/v5.0/v5.1 encontrado além disso. |
| 11 (fechamento) | 2026-08-24 | 0 aberto | 0 aberto | 0 aberto | 0 aberto | **Todos os 2 achados decididos/corrigidos** na mesma sessão (1 decisão de comportamento vs. doc levada ao usuário com avaliação de custo explícita antes da escolha final). Nenhum achado ficou aberto sem decisão. Complexidade permanece convergida (fora de rotação). Usabilidade e observabilidade seguem sem 2 rodadas limpas consecutivas - ambas continuam achando itens reais, mas de severidade decrescente e cada vez mais específicos (não mais classes de bug novas, e sim precisão de caracterização em casos de borda já mapeados). Verificado: 194/194 testes net10.0 (201 no fechamento do Round 10, -7 pela remoção de testes redundantes), build limpo (0 warnings, 0 errors) em toda a solução a cada etapa. |
