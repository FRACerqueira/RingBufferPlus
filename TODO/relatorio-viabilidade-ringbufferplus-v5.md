# RingBufferPlus — Relatório de Viabilidade de Produto (rumo à v5.1.0)

**Nota de enquadramento (2026-08-20):** a v5.0.0 **já foi publicada** (tag e CHANGELOG datados de 2026-08-12) e não é mais o alvo desta auditoria — não é algo a "segurar" ou "re-cortar". O alvo real é a **próxima release, v5.1.0**: um minor que carrega correções de comportamento e, deliberadamente, uma quebra pontual e pequena (ver [ADR004 V02](../doc/adr/ADR004V02-semantic-versioning-policy-and-fluent-api-stability.md) para a exceção de versionamento). As seções abaixo usam a linguagem original da auditoria (que tratava a v5.0.0 como "o release a proteger") — o veredito prático não muda, só o quadro: em vez de "não promover a v5.0.0", leia-se "não fechar/publicar a v5.1.0 até que os itens abaixo estejam resolvidos".

**Escopo:** estabilidade, resiliência e usabilidade da v5.0.0, com as correções endereçadas na v5.1.0
**Método:** três passes de auditoria independentes (sem contexto compartilhado entre si), cada um verificado em código em execução, não apenas por leitura de código
**Estado do repositório:** inalterado — toda a verificação rodou a partir de probes descartáveis fora do repositório, referenciando a biblioteca compilada
**Veredito geral: NÃO FECHAR A v5.1.0** até que os bloqueadores da [§3](#3-plano-de-ação-recomendado) estejam corrigidos e reverificados.

**Status de execução (2026-08-20):** **os 6 bloqueadores P0 (§3) estão todos corrigidos e verificados** — F1, F2, F3, F4, F5, F10, R1, R2, R3, R12, U-01 e U-02 fechados com protocolo vermelho/verde (ou, quando a corrida não reproduziu mesmo com esforço redobrado, com o mecanismo nomeado explicitamente per regra 5 do CLAUDE.md). Todo o P1 concluído exceto U-07 (ver P2 Decisão C). P2 Decisão A implementada (`LockWhenScaling` removido — [ADR007 V02](../doc/adr/ADR007V02-redesign-of-the-public-fluent-api-surface.md)); Decisões B e C confirmadas mas ainda não implementadas. Suíte completa: 90/90 testes, 0 warnings, net8.0/net9.0/net10.0. Acompanhar o progresso item-a-item em [`plano-de-acao.md`](./plano-de-acao.md).

---

## Sumário

1. [Sumário executivo](#1-sumário-executivo)
2. [O problema central](#2-o-problema-central-uma-causa-raiz-duas-confirmações-independentes)
3. [Plano de ação recomendado](#3-plano-de-ação-recomendado)
4. [Decisões que precisam de você, não de um ajuste de texto](#4-decisões-que-precisam-de-você-não-de-um-ajuste-de-texto)
5. [Achados detalhados — Estabilidade](#5-achados-detalhados--estabilidade-concorrência-e-correção)
6. [Achados detalhados — Resiliência](#6-achados-detalhados--resiliência-comportamento-sob-falha)
7. [Achados detalhados — Usabilidade](#7-achados-detalhados--usabilidade-superfície-de-api-e-documentação)
8. [Metodologia](#8-metodologia)
9. [Veredito final](#9-veredito-final)

---

## 1. Sumário executivo

| Eixo | Veredito | Manchete |
|---|---|---|
| **Estabilidade** (concorrência, correção) | **NÃO PRONTO** | Uma factory que lança exceção mata o engine permanentemente; todo método público assíncrono (`Warmup`/`Acquire`/`Switch`/`Dispose`) pode então travar para sempre. Confirmado em execução, **inclusive em capacidade fixa**, não só elástica. |
| **Resiliência** (comportamento sob falha) | **NÃO PRONTO** | Mesmo defeito, alcançado pelo caminho realista: RabbitMQ, banco de dados ou qualquer factory de recurso externo real falha *lançando exceção*, não por cancelamento — que é o único modo de falha para o qual o engine foi construído para sobreviver. |
| **Usabilidade** (API e documentação) | **PRONTO COM RESSALVAS** | A profundidade da documentação é genuinamente boa (10 guias, ADRs, referência gerada, zero links quebrados). Mas as duas coisas que um novo adotante toca primeiro — o Quickstart do README e a instrução principal do guia de DI — falham ao copiar e colar. Correções são só texto, sem risco de código, cerca de um dia de trabalho. |

**Por que isso importa comercialmente:** os defeitos se concentram exatamente nas funcionalidades que diferenciam esta biblioteca de um pool de objetos comum — autoscaling elástico e pooling de recursos externos (canais RabbitMQ, conexões de banco). Eles se manifestam como **comportamento silenciosamente errado**, não como exceções: um engine morto, um contador de capacidade que mente, um scale-up que silenciosamente nunca acontece, métricas que reportam sucesso para operações que falharam. "Silenciosamente errado" é o modo de falha mais caro de diagnosticar em produção, e hoje não há nenhum teste que o proteja — a suíte de testes não tem, em lugar nenhum, um teste com uma factory que lança exceção.

**A parte tranquilizadora:** a arquitetura em si é sólida. Os dois passes técnicos confirmaram, de forma independente, que a aposta arquitetural central do rewrite v5 (um único consumidor em canal (channel) que possui todo o estado de escala) realmente funciona — nenhuma atualização perdida, nenhum estado corrompido, nenhum deadlock encontrado na lógica de escala em si. O que falta é um perímetro defensivo ao redor desse engine, e a correção é pequena, local e não exige redesenhar nada.

---

## 2. O problema central: uma causa raiz, duas confirmações independentes

O passe de Estabilidade e o passe de Resiliência trabalharam a partir de arquivos diferentes, testes diferentes e modelos mentais diferentes — e convergiram, de forma independente, para a mesma linha de código.

> **Todo caminho de erro da factory até o loop de comandos do engine filtra exclusivamente por `OperationCanceledException`.**
> `RingBufferManager.cs:513,516,528,363,370,574,577` — todo `catch` na cadeia entre `Factory(...)` e o `await foreach` do engine é `catch (OperationCanceledException)`. Nada além disso é capturado em nenhum ponto desse caminho.

Factories reais não falham só por cancelamento. Uma factory de conexão de banco de dados lança `SocketException`; o RabbitMQ.Client lança `BrokerUnreachableException`, `AlreadyClosedException`, `OperationInterruptedException`; código de usuário lança `NullReferenceException`, `InvalidOperationException`. Qualquer uma dessas escapa de todos os catches, derruba (faults) o `_engineTask`, e a única thread dona de todo o estado de escala desaparece pelo resto da vida da instância — sem exceção, sem log, sem métrica marcando o momento em que isso aconteceu.

**Consequências confirmadas, reproduzidas em execução, em um buffer `FixedCapacity(3)` simples (ou seja, isto não é um defeito exclusivo de elástico/autoscale):**

| Gatilho | Comportamento observado |
|---|---|
| Factory lança exceção durante `BuildWarmupAsync` | `WarmupAsync()` trava para sempre — nenhum timeout se aplica aqui |
| ...depois `AcquireAsync()` | trava para sempre |
| ...depois `DisposeAsync()` | trava para sempre — **o shutdown do host nunca completa** |
| Factory lança exceção *depois* de um warmup saudável | `SwitchToAsync()` trava para sempre; `DisposeAsync()` em vez disso **lança a exceção antiga da factory**, abortando a limpeza antes de drenar os itens do pool |
| Uma chamada de `Invalidate()` durante a instabilidade | `CurrentCapacity` continua reportando o número antigo, enquanto o pool está de fato com um item a menos — permanentemente, mesmo depois que a factory se recupera |
| Autoscale-on-fault habilitado | depois que o engine morre, novos timeouts de acquire produzem **zero** scale-up — o autoscale é silenciosa e permanentemente desabilitado |

**Duas leituras, ambas corretas, ambas necessárias:**
- O passe de **Estabilidade** enquadra isso como uma ausência de fronteira de exceção (exception boundary) no próprio engine de concorrência — o defeito é que a máquina de estados não tem caminho de falha para seus próprios comandos, e é isso que transforma "a factory lançou uma exceção" em "toda API pública trava para sempre", incluindo `Warmup`, `Switch` e `Dispose`, nenhum dos quais tem a ver diretamente com a factory.
- O passe de **Resiliência** enquadra isso pelo lado do gatilho — uma indisponibilidade de broker/banco, que é a condição operacional normal que esta biblioteca promete suportar (ver `doc/guides/usage-rabbitmq.md`, que promete explicitamente degradação graciosa em caso de queda de conexão). Essa promessa não se sustenta hoje.

Corrigir a fronteira de exceção ausente uma única vez, no loop do engine, resolve as duas leituras simultaneamente — ver Ação 1 abaixo.

---

## 3. Plano de ação recomendado

Ordenado pelo que precisa acontecer antes da próxima afirmação pública de "pronto para produção".

### P0 — Bloqueadores de release

| # | Correção | Fecha | Por que é P0 |
|---|---|---|---|
| 1 ✅ | **[CORRIGIDO]** Capturar `Exception` (não só `OperationCanceledException`) em `ProcessCommandAsync`, `RunEngineAsync`, `CreateSingleReplacementAsync`. No catch: logar, falhar os `TaskCompletionSource`s de `Accepted`/`Completion` do comando, descartar itens parcialmente criados, e **manter o loop rodando**. | Estabilidade F1, Resiliência R1 | O defeito de uma linha, 100% reproduzível, descrito na §2. Tudo o que decorre dele (travamentos, perda silenciosa de capacidade, autoscale morto) desaparece assim que o loop sobrevive a uma exceção lançada. |
| 2 ✅ | **[CORRIGIDO — apenas a parte de token; ver [plano-de-acao.md](./plano-de-acao.md) P0#2]** Fazer todo `await` sobre um `TaskCompletionSource` do engine (`WarmupCoreAsync`, `SwitchToAsync`) carregar `_lifetime.Token`; ~~ao sair do engine, drenar `_commands` e falhar o TCS de cada comando restante em vez de descartá-los silenciosamente~~ (decidido como não necessário — ver plano de ação). | Estabilidade F4, Resiliência R1/R2 | Sem isso, uma exceção de factory ou um shutdown cancelado ainda deixam waiters individuais (não só chamadas futuras) travados mesmo após a correção 1. |
| 3 ✅ | **[CORRIGIDO]** Substituir as guardas não atômicas `bool _disposed` por `Interlocked.Exchange` tanto em `RingBufferValue.DisposeAsync` (`RingBufferValue.cs:63-71`) quanto em `RingBufferManager.DisposeAsync` (`RingBufferManager.cs:240-241`). | Estabilidade F2, F10 | F2 está **confirmado em execução**: double-dispose concorrente entrega a *mesma instância do pool* para duas chamadas simultaneamente (~3,8% de incidência em um probe de 2000 iterações). Para uma conexão de banco ou canal pooled, isso é uso intercalado por dois chamadores — um bug de integridade de dados, não um vazamento. |
| 4 ✅ | **[CORRIGIDO]** Aplicar o timeout do pulso do heartbeat com `Task.WhenAny(work, Task.Delay(PulseHeartBeat))` (ou `.WaitAsync(token)`) em vez de passar um `CancellationToken` para `Task.Run`, que não cancela de fato um delegate em execução. | ~~Estabilidade F3,~~ Resiliência R3 | **Confirmado em execução**: um callback de heartbeat que bloqueia para a bomba de heartbeat para sempre e prende um item do pool — exatamente o modo de falha que um heartbeat existe para detectar, não detectado porque a guarda feita para isso é código inalcançável. **Nota de correção (2026-08-20): esta linha, na síntese original, citava "Estabilidade F3" por engano — F3 é um bug diferente (ver linha 5 abaixo) que esta ação não fecha. Removido daqui; F3 será fechado pela Ação 5.** |
| 5 ✅ | **[CORRIGIDO — também fecha F3, realocado do item #4]** Tornar a limpeza do `DisposeAsync` incondicional: alargar o catch do `Task.WhenAll(pending)` para `Exception`, e mover a drenagem de itens + o dispose de `_lifetime`/`_meter`/`_activitySource` para um `finally`. Também descartar o item no handler de `ChannelClosedException` engolido em `RingBufferManager.cs:345`, em vez de perdê-lo. | Estabilidade F3, F5, Resiliência R2/R12 | Confirmado: hoje, `DisposeAsync` pode lançar uma exceção antiga **antes** de drenar os itens do pool, vazando cada um deles mais o `Meter`/`ActivitySource` do próprio buffer. |
| 6 ✅ | **[CORRIGIDO]** Corrigir o Quickstart do `README.md` (adicionar `CancellationToken cancellation = default;`) e remover, do guia de DI, a instrução de injetar `IRingBufferManualScaleService<T>` diretamente (`doc/guides/usage-dependency-injection.md:46,54` — esse tipo nunca é registrado e a resolução lança exceção). | Usabilidade U-01, U-02 | Ambos são a primeira coisa que um novo adotante copia. Correções puramente de texto, risco de código zero. |

**Antes de reafirmar "pronto para produção", adicionar testes de regressão para:** uma factory que lança uma `Exception` comum (nenhum existe hoje — busquei em toda a suíte), double-dispose concorrente de um lease, dispose durante um warmup em andamento com timeout de teste rígido (o teste de contrato `RingBufferContractTests.cs:275` já cobre esse cenário, mas hoje passa só porque o engine geralmente vence uma corrida — é um **travamento latente de CI**, não uma garantia verificada), e dispose durante heartbeat ativo.

### P1 — Mesmo release ou follow-up imediato (sem mudança de comportamento visível ao usuário, ou uma mudança claramente contida)

| # | Correção | Fecha |
|---|---|---|
| 7 ✅ | **[CORRIGIDO]** Marcar `scale.operations`/`scale.duration` com um resultado (sucesso/falhou/expirou), e definir `ActivityStatusCode.Error` no span de scale quando uma tentativa falhar ou expirar. | Resiliência R8 |
| 8 ✅ | **[CORRIGIDO]** Desacoplar o prazo do scale-up da janela de amostragem (`SamplesBase`), ou documentar/validar que ele precisa exceder `delta × FactoryTimeout`; manter itens parcialmente criados em um timeout parcial em vez de descartar todos; redimensionar `samples/RingBufferPlusRabbitSample` (sua própria configuração torna o scale-up estruturalmente impossível acima de ~500ms por chamada de factory). | Resiliência R5 |
| 9 ✅ | **[CORRIGIDO]** Corrigir a fórmula do alvo de scale-up para que o autoscale não fique permanentemente desabilitado quando `initialCapacity == minCapacity` (uma configuração legal e natural). | Resiliência R4 |
| 10 ✅ | **[CORRIGIDO]** Adicionar ao `CHANGELOG.md`, na seção "Breaking changes v5.0.0", uma tabela de nomes de interface antigo→novo, além de uma nota de que `SwitchToAsync` não existe mais em `IRingBufferService<T>`. README e ADR004 chamam essa seção de *única* referência de migração — hoje ela omite as duas quebras com maior chance de atingir código real de usuários v4. | Usabilidade U-08 |
| 11 🔎 | **[5 de 6 CORRIGIDOS]** Corrigir seis afirmações de documentação erradas/ambíguas: a condição do alvo de scale-up (U-03 ✅), a doc de retorno do `SwitchToAsync` (ainda descreve o v4, U-04 ✅), a afirmação de "retorna imediatamente" que na verdade pode bloquear por ~30s (U-05 ✅), o comentário do sample RabbitMQ ensinando a semântica v4 de `LockWhenScaling` para um cenário que hoje é um no-op (U-06 ✅), o off-by-one do limiar de falhas do autoscale repetido em quatro lugares (U-07 ⏳ — depende da Decisão C em §4), o tipo de exceção errado no guia de autoscale (U-21 ✅). | Usabilidade U-03–U-07, U-21 |

### P2 — Escalado para você (ver §4 para a análise completa de trade-off — não resolver como ajuste de doc)

- `LockWhenScaling()` é um método público em `IRingBufferAutoScaleBuilder<T>` que é um no-op documentado — e já enganou o autor do próprio sample RabbitMQ deste repositório.
- Um `WarmupAsync` que falha destrói permanentemente a instância (exceção cacheada via `Lazy<Task>`), enquanto todo guia de DI recomenda registrar o buffer como singleton.
- A comparação de contagem de falhas do autoscale (`>` vs. o "após a primeira falha" documentado) — corrigir o código para bater com quatro locais de documentação, ou corrigir os quatro locais de documentação para bater com o código.

### P3 — Passe de documentação/higiene de acompanhamento (sem urgência de release)

Itens Médios/Baixos restantes: Estabilidade F6, F7, F9, F11; Resiliência R6, R9, R10, R11; Usabilidade U-09 a U-24 (exceto os já listados acima). Ver §5–§7 para a lista completa; nenhum desses bloqueia um release sozinho.

---

## 4. Decisões que precisam de você, não de um ajuste de texto

Seguindo a regra padrão para este tipo de revisão: um achado cuja correção carrega um trade-off real é escalado com opções, não resolvido silenciosamente. Três se qualificam.

### 4.1 — `LockWhenScaling()` no builder de autoscale: remover, ou manter como no-op documentado?

`IRingBufferAutoScaleBuilder.cs:65-73` afirma que o método é "aceito mas não tem efeito observável. Mantido aqui apenas para que a cadeia fluente continue compilando sem alteração." O ADR007 se propôs explicitamente a eliminar exatamente esse tipo de armadilha — "interações silenciosas em runtime", "um `false` silencioso em runtime" (ADR007 §28, §38, §54) — e até esboça `LockWhenScaling` como algo que deveria existir só onde faz sentido (§55). Concretamente, isso já enganou alguém: `samples/RingBufferPlusRabbitSample/Program.cs:87-94` chama `.LockWhenScaling()` imediatamente antes de `.AutoScaleAcquireFault()`, com um comentário afirmando que a execução demonstra o comportamento de lock — não demonstra nada, porque a configuração é inerte nessa posição (ver U-06/U-10 para detalhes).

- **Opção A — remover de `IRingBufferAutoScaleBuilder<T>`.** Transforma o erro em um erro de compilação no ponto de chamada, que é o sinal em tempo de compilação que o ADR007 foi escrito para garantir. **Custo:** uma mudança quebradora (breaking change) na superfície fluente, logo após um release já justificado por "breaking changes autorizadas" (ADR006) — o timing pode ou não ser o certo.
- **Opção B — manter, mas dificultar cair na armadilha:** desencorajamento no estilo `[Obsolete]` mais uma nota de revisão no ADR007 registrando o desvio. **Custo:** a superfície pública mantém um método cuja única propriedade documentada é não fazer nada.

### 4.2 — É aceitável que "falha no warmup destrói permanentemente a instância", dado que todo guia manda registrar como singleton no DI? ✅ DECIDIDO E CORRIGIDO

`RingBufferManager.cs:136` envolve o warmup em `Lazy<Task>`, que cacheia a exceção — confirmado intencional pelo próprio comentário do código ("uma instância cujo warmup lança exceção está permanentemente quebrada por design; construa uma nova instância para tentar de novo"). Nada na documentação XML pública de `WarmupAsync` ou `BuildWarmupAsync` diz isso, e tanto `doc/guides/usage-dependency-injection.md` quanto `concepts.md` empurram o registro como singleton. Consequência prática: uma falha transitória na inicialização do host (um banco de dados ainda não aceitando conexões, um broker ainda subindo) desabilita permanentemente o pool pelo resto da vida do processo, recuperável só com um restart completo.

- **Opção A — manter o comportamento, documentá-lo claramente** em `WarmupAsync`/`BuildWarmupAsync` e no guia de DI, deixando operadores decidirem se isso é aceitável para suas garantias de ordem de inicialização.
- **Opção B — adicionar uma forma de tentar de novo** (por exemplo, um caminho de re-warmup, ou orientação para registrar um delegate de factory que já tenta novamente com backoff antes de entregar o controle ao `RingBufferManager`).

**Escolhida a Opção B.** `WarmupAsync()` agora instala uma nova tentativa (CAS sobre um novo `Lazy<Task>`) quando a anterior falhou, e tenta de novo do zero; `AcquireAsync`/`SwitchToAsync` (gatilho implícito) deliberadamente não retentam sozinhos, para não transformar tráfego normal em uma tempestade de retries contra um `Factory` ainda quebrado. Justificado por [ADR011V01](../doc/adr/ADR011V01-retry-path-for-a-failed-warmup-async-instead-of-a-permanently-broken-instance.md). Doc XML de `WarmupAsync` atualizada para descrever o novo comportamento. Ver [plano-de-acao.md](./plano-de-acao.md) P2 Decisão B.

### 4.3 — Limiar de falhas do autoscale: corrigir o código, ou corrigir quatro locais de documentação?

`RingBufferManager.cs:400`: `if (_faultCount > NumberFault && ...)`. Com o padrão `numberOfFaults: 1`, o scale-up de fato dispara na **segunda** falha, não na primeira. Quatro locais diferentes documentam "após a primeira falha" ou uma fórmula de tempo de reação que assume isso (`IRingBufferElasticBuilder.cs:98`, a referência gerada, `doc/guides/usage-elastic-autoscale.md`, e o `CHANGELOG.md` — o documento posicionado como única referência de migração v4→v5). O próprio `samples/RingBufferPlusBasicTriggerScale` deste repositório passa `AutoScaleAcquireFault(0)` especificamente para obter "dispara na primeira falha" — forte evidência de que a intenção documentada era `>=`, não `>`.

- **Opção A — mudar a linha 400 para `>=`.** Bate com toda afirmação documentada. **Custo:** uma mudança de comportamento no timing do autoscale para quem hoje depende do off-by-one como está (improvável, mas não zero).
- **Opção B — corrigir os quatro locais de documentação** para declarar a regra real ("dispara na falha de número `numberOfFaults + 1`; passe `0` para primeira-falha"). **Custo:** nenhum além do ajuste de texto, mas a semântica contraintuitiva de "`0` significa primeira falha" permanece.

---

## 5. Achados detalhados — Estabilidade (concorrência e correção)

**Veredito do passe: NÃO PRONTO.** A arquitetura de concorrência (o canal de consumidor único do ADR001) é sólida — nenhuma atualização perdida, nenhum estado corrompido, nenhum deadlock encontrado na lógica de escala propriamente dita. O que falta é o perímetro defensivo: uma fronteira de exceção ao redor do loop do engine, guardas de dispose atômicas, e um timeout de heartbeat que seja de fato aplicado. Todas são correções pequenas, locais e testáveis de forma independente.

| ID | Severidade | Achado | Localização | Recomendação |
|---|---|---|---|---|
| **F1** | **Crítica — ✅ CORRIGIDO** | Nenhuma fronteira de exceção no loop do engine; qualquer exceção que não seja de cancelamento vinda da factory mata permanentemente o `_engineTask`, órfão de todo waiter atual e futuro. Ver [§2](#2-o-problema-central-uma-causa-raiz-duas-confirmações-independentes). | `RingBufferManager.cs:513,516,528,363,370,382` | Capturar `Exception`, falhar os waiters, manter o loop vivo (Ação 1). **Corrigido — ver [plano-de-acao.md](./plano-de-acao.md) P0#1.** |
| **F2** | **Alta — confirmada em execução — ✅ CORRIGIDO** | A guarda de `RingBufferValue.DisposeAsync` é um `bool` não atômico; double-dispose concorrente duplica um lease. Probe de 2000 iterações: 76 leases duplicados (~3,8%), incluindo um caso confirmado de dois chamadores simultâneos segurando instâncias idênticas por `ReferenceEquals`. | `RingBufferValue.cs:24,63-71` | `Interlocked.Exchange` em uma flag `int` (Ação 3). **Corrigido — ver [plano-de-acao.md](./plano-de-acao.md) P0#3.** |
| **F3** | **Alta — confirmada em execução — ✅ CORRIGIDO** | `DisposeAsync` lança `ObjectDisposedException` quando um pulso de heartbeat está em andamento (~18% de incidência com pulso de 15ms em um probe de 400 iterações), abortando a limpeza antes de drenar os itens do pool. **Não é o mesmo bug que R3** (callback bloqueante — já corrigido); F3 é a corrida entre `_disposed` virar `true` e o `AcquireAsync` interno do heartbeat rodar. | `RingBufferManager.cs:151,615,640-643,270-275` | Capturar `ObjectDisposedException` no caminho do heartbeat; tornar a limpeza do `DisposeAsync` incondicional (Ação 5). **Corrigido — ver [plano-de-acao.md](./plano-de-acao.md) P0#5.** |
| **F4** | **Alta — confirmada em execução — ✅ CORRIGIDO** | A saída do engine por cancelamento descarta todos os comandos na fila sem falhar seus `TaskCompletionSource`s. `WarmupCoreAsync` aguarda um deles sem token — um `DisposeAsync` chamado sobre uma instância ainda não aquecida pode travar para sempre. O teste de contrato `RingBufferContractTests.cs:275` cobre esse cenário, mas passa só por vencer uma corrida; reproduziu como travamento na 2ª tentativa nesta auditoria. | `RingBufferManager.cs:243,244,255,303-304,357,370-373` | Token em todo await de TCS; drenar-e-falhar na saída do engine (Ação 2). **Corrigido (token nos awaits — ver [plano-de-acao.md](./plano-de-acao.md) P0#2, que também registra uma correção de entendimento sobre a mecânica exata dessa corrida).** |
| **F5** | **Média-Alta — ✅ CORRIGIDO** | Itens retirados no momento do shutdown nunca são descartados — o handler de `ChannelClosedException` do `TurnbackAsync` engole o retorno com um comentário "ignore" e nenhuma chamada a `DisposeItemAsync`. | `RingBufferManager.cs:277-281,337,345-348` | Descartar o item nesse handler (Ação 5). **Corrigido — ver [plano-de-acao.md](./plano-de-acao.md) P0#5.** |
| **F6** ✅ CORRIGIDO | **Média** | A guarda `_scaling` em `ProcessTickAsync` é código morto inalcançável (grep confirma apenas 4 ocorrências, todas consistentes com "sempre falso"). Consequência: um scale-up lento pode deixar a janela de amostragem preenchida com leituras tiradas quase no mesmo instante, degradando a mediana para quase uma única amostra — o que pode disparar um scale-down imediato logo após um scale-up terminar. **Corrigido:** a checagem de `_scaling` foi movida para o produtor (`RunSampleTickAsync`), que agora pula a escrita do `Tick` no canal enquanto uma escala está em andamento (evita o acúmulo/burst); `_samples` é limpo ao final de `MoveToCapacityAsync`, para a janela pós-escala começar do zero. Ver `plano-de-acao.md` P3#F6. | `RingBufferManager.cs:64,420-424,458,486,646-662` | Remover o ramo morto, ou mover a guarda para onde possa de fato rodar; marcar com timestamp/descartar amostras obsoletas. |
| **F7** ✅ CORRIGIDO | **Baixa-Média** | `_faultCount` nunca reseta enquanto a capacidade está travada em `MaxCapacity` (só reseta dentro do ramo que exige `CurrentCapacity != MaxCapacity`), então falhas continuam se acumulando; após um scale-down posterior, o próximo timeout de acquire dispara imediatamente o scale para o máximo de novo, produzindo oscilação (flapping). Relacionado à questão do limiar em §4.3. **Corrigido junto com R7** — ver `plano-de-acao.md` P3#F7/R7. | `RingBufferManager.cs:399-405` | Resetar/limitar o contador independentemente de um scale ser possível no momento. |
| **F8** | **Média** | O ADR001 cita `LockWhenScaling` como a mitigação para quem quer acquire serializado contra a escala; a v5 removeu essa capacidade (`LockWhenScaling` agora só afeta se o *chamador* do `SwitchToAsync` aguarda a conclusão) e a mudança está registrada só em um comentário de código, não no ADR. | ADR001 §36; `RingBufferManager.cs:13-17,224` | Revisar/suceder o ADR001 para declarar a semântica v5; decidir deliberadamente se um mecanismo substituto é necessário. Ver também §4.1. |
| **F9** ✅ CORRIGIDO | **Baixa-Média** | O contrato de retorno documentado de `IRingBufferManualScaleService.SwitchToAsync` ("false quando já na capacidade ou quando um scale já está em andamento") não bate com a implementação (não existe rejeição de "já em andamento" — ele só entra na fila). Mesmo defeito que Usabilidade U-04. **Corrigido junto com U-04** (mesmo defeito, mesma correção): a doc XML agora descreve o comportamento real (fila, não rejeição) — confirmado que bate com `RingBufferManager.cs:447-465` hoje. Sem mudança de código adicional necessária. | `Commands/IRingBufferManualScaleService.cs:23-26`; `RingBufferManager.cs:386-395` | Implementar a rejeição documentada, ou corrigir a doc — escolher uma (relacionado ao mesmo tipo de alinhamento código/doc de §4.3, com menor impacto). |
| **F10** | **Baixa — suspeita, não reproduzida — ✅ CORRIGIDO (sem teste vermelho/verde)** | A própria guarda `_disposed` do `RingBufferManager.DisposeAsync` segue o mesmo padrão não atômico de F2; um probe de 300 iterações de dispose concorrente produziu 0 exceções (a janela é estreita), então isso é sinalizado como higiene latente, não um bug demonstrado hoje. Uma segunda tentativa durante a correção (1000 iterações, threads dedicadas, ~12 min) também produziu 0 exceções. | `RingBufferManager.cs:62,240-241,283` | Mesma correção com `Interlocked` de F2, mesmo idioma, barato de fazer junto. **Corrigido — ver [plano-de-acao.md](./plano-de-acao.md) P0#3 para o registro explícito de "vermelho não alcançado" (regra 5, passo 3).** |
| **F11** ✅ CORRIGIDO | **Baixa** | `ScaleDownMin` é calculado e propagado, mas nunca lido por `AutoScaleDecision.EvaluateScaleDown` — estado morto (grep confirma exatamente 3 ocorrências, uma sendo um fixture de teste). **Corrigido:** removido (Opção A) — não há semântica coerente para "reduzir a partir do mínimo" já que `MinCapacity` é o piso validado do buffer. Ver `plano-de-acao.md` P3#F11/R9. | `RingBufferBuilder.cs:207,290`; `RingBufferManager.cs:89`; `AutoScaleDecision.cs:55-56` | Remover, ou implementar — não deixar silenciosamente inerte. |
| **F12** — **novo, Rodada 2 (2026-08-20) — ✅ CORRIGIDO** | **Alta — confirmada em execução (567 ocorrências em 5 timeouts, ~8s de probe)** | No timeout do heartbeat, `Invalidate()` marca `SkipTurnback=true` e o `await using` disponível logo depois já dispara `TurnbackAsync` → `DisposeItemAsync` **imediatamente** — destruindo o recurso pooled de verdade enquanto o callback órfão (que não pode ser cancelado, roda numa thread própria) pode ainda estar lendo/escrevendo nele. É uma corrida de use-after-dispose no recurso do próprio usuário (conexão de banco, canal RabbitMQ), não só um problema de bookkeeping interno. **Só ficou visível nesta rodada** porque antes da correção do R3/P0#4 (rodada 1), o caminho de timeout do heartbeat era código morto (o cancelamento não cancelava de fato o delegate em execução) — tornar o timeout real é exatamente o que expõe essa consequência. O teste de regressão existente (`RingBufferContractTests.cs:621`) usa `T=int`, que não é `IDisposable`, então não conseguia detectar isso. **Corrigido:** as duas responsabilidades foram separadas — `ReplaceOne` é enfileirado imediatamente no timeout (mantendo a garantia original do R3/P0#4 de que a capacidade não fica presa), mas o `DisposeItemAsync` do objeto físico foi movido para dentro da continuação `heartbeatWork.ContinueWith`, só executando depois que o callback órfão de fato termina. Ver `plano-de-acao.md` P4#F12. | `RingBufferManager.cs:773-831` (`RunHeartbeatAsync`), `RingBufferValue.cs` (`Invalidate`/`DisposeAsync`), `RingBufferManager.cs:388-409` (`TurnbackAsync`) | Não descartar/substituir o item sincronamente no timeout — mover o `Invalidate()`/enfileiramento de `ReplaceOne` para dentro da continuação `heartbeatWork.ContinueWith`, para o recurso só ser destruído depois que o callback órfão de fato terminar (aceitando um slot temporariamente "presa" em vez de uma corrida de uso ativo). |
| **F13** — **novo, Rodada 2 (2026-08-20) — ✅ REMOVIDO (2026-08-21)** | **Baixa — suspeita, não reproduzida (é uma questão de alcançabilidade, não de timing)** | O `catch (OperationCanceledException)` por-comando em `RunEngineAsync` parece ser código morto sob o `ProcessCommandAsync` atual: rastreando os 5 ramos de `EngineCommandKind`, nenhum consegue propagar uma exceção (de qualquer tipo) até esse catch externo — todos já capturam localmente. Mesma classe do F6 (ramo defensivo que deixou de ser alcançável depois que uma correção mais específica foi empilhada por cima). **Reavaliado na Rodada 3 (Estabilidade):** inalterado — ainda código morto, ainda sem urgência. **Decisão do mantenedor (2026-08-21, limpeza de backlog entre rodadas):** remover agora, em vez de continuar registrando. **Removido:** o `try/catch` por-comando ao redor de `ProcessCommandAsync(cmd)` em `RunEngineAsync` foi deletado — o `await foreach` agora chama `ProcessCommandAsync(cmd)` diretamente. Verificado antes/depois (remoção pura de código morto, sem mudança de comportamento — protocolo vermelho→verde não se aplica aqui): suíte net10.0 121/121 antes e depois. | `RingBufferManager.cs` (`RunEngineAsync`, catch por-comando) | Removido — nenhuma recomendação pendente. |
| **F14** — **novo, Rodada 2 (2026-08-20) — ✅ FECHADO, sem evidências (2026-08-21)** | **Baixa — mecanismo nomeado, explicitamente não reproduzido (regra 5, passo 3)** | O retry do `WarmupAsync()` (ADR011, CAS sobre um novo `Lazy<Task>`) lê `_lifetime.Token` de forma síncrona antes de qualquer `await`. Em teoria, se `DisposeAsync` já tivesse terminado completamente nesse exato instante, isso lançaria `ObjectDisposedException` nomeando o `CancellationTokenSource` interno, não o manager — furando o contrato documentado (`ObjectDisposedException.ThrowIf(_disposed, this)`). Probe de 3000 iterações competindo `WarmupAsync()` (retry) contra `DisposeAsync()` concorrente: **0 ocorrências**. Razão provável: o prefixo síncrono do retry (checagem→CAS→invocação) não tem nenhum `await` antes de tocar o token, então as duas operações nunca têm sobreposição de janela suficiente. **Reavaliado na Rodada 3 (Estabilidade):** inalterado — ainda não reproduzido, ainda registrado só por completude. **Decisão do mantenedor (2026-08-21, limpeza de backlog entre rodadas):** fechar como "sem evidências" — 3000 iterações de probe e 2 rodadas de reavaliação sem uma única ocorrência não justificam continuar carregando isto como item aberto; nenhuma mudança de código feita. | `RingBufferManager.cs:249-269,358` | Nenhuma — fechado sem evidências, não é um defeito demonstrado. |
| **F15** — **novo, Rodada 3 (2026-08-20) — ✅ CORRIGIDO** | **Alta — confirmada em execução (probe determinístico)** | A correção do F12 (Rodada 2) separou "substituir a capacidade" (imediato) de "descartar o objeto físico" (deferido até o callback órfão terminar), mas o guard do catch que decide qual caminho seguir — `catch (OperationCanceledException) when (!_lifetime.IsCancellationRequested)` — excluía exatamente o caso em que `_lifetime` é a causa do cancelamento (um `DisposeAsync()` comum, não um timeout de pulso) enquanto o callback ainda está rodando. Esse caso caía no `catch (Exception ex)` genérico seguinte, que descarta o recurso imediatamente — reabrindo a mesma corrida de use-after-dispose que o F12 tinha corrigido, agora só para o caminho de shutdown normal. Confirmado com um probe: `DisposeAsync()` chamado ~5s dentro de um orçamento de pulso de 5s (bem dentro do budget, não um timeout) ainda descartava o recurso enquanto o callback continuava rodando. **Corrigido:** o guard trocou de `!_lifetime.IsCancellationRequested` para `!heartbeatWork.IsCompleted` — a decisão agora é "o callback ainda está rodando?", não "quem cancelou?" — cobrindo os dois casos (timeout de pulso do F12, e shutdown normal do F15) com o mesmo caminho de dispose deferido. O nível de log (`LogError` vs. `LogMessage`) ainda distingue timeout real de shutdown normal. Achado de forma independente também pelo passe de Resiliência da mesma rodada ("Finding B"), confirmando a mesma causa raiz por um ângulo diferente. | `RingBufferManager.cs` (`RunHeartbeatAsync`, catch do timeout de heartbeat) | Trocar o guard do catch de "quem cancelou" para "o callback ainda está rodando" (`!heartbeatWork.IsCompleted`), cobrindo os dois casos com o mesmo caminho de dispose deferido do F12. |
| **F16** — **novo, Rodada 4 (2026-08-21) — ✅ CORRIGIDO** | **Média-Alta — confirmada em execução (probe, gap de 2688ms medido)** | A continuação de dispose deferido do F12/F15 (`heartbeatWork.ContinueWith(...)`) era fire-and-forget — `DisposeAsync()` só aguarda `_heartbeatTask` (o loop do heartbeat em si), nunca essa continuação. `RunHeartbeatAsync` retorna quase imediatamente após agendar a continuação (não espera `heartbeatWork` terminar), então `DisposeAsync()` podia retornar enquanto o recurso pooled de verdade (conexão de banco, canal RabbitMQ) ainda não tinha sido disposed. Não é só atraso: se o processo termina logo depois do `DisposeAsync()` retornar, esse recurso nunca é limpo. **Corrigido:** a continuação agora é rastreada (`_pendingHeartbeatDisposals`, um `ConcurrentBag<Task>`, com `.Unwrap()` para o Task rastreado refletir a conclusão real, não só o agendamento) e `DisposeAsync()` aguarda todas as pendentes, com um limite próprio (`PulseHeartBeat` de novo, já que o callback não pode ser cancelado e pode estar permanentemente preso) — timeout vira `LogMessage`, não erro. | `RingBufferManager.cs` (`RunHeartbeatAsync`'s deferred-dispose continuation, `DisposeAsync`) | Rastrear a continuação numa coleção aguardada no `finally` do `DisposeAsync`, com timeout próprio. |
| **F17** — **novo, Rodada 4 (2026-08-21) — ✅ CORRIGIDO** | **Média — confirmada em execução (probe, ~10-14% de incidência com pulso amplificado)** | `RunHeartbeatAsync`'s própria chamada interna de acquire (`AcquireForHeartbeatAsync`) pode lançar `ObjectDisposedException` numa janela estreita entre `DisposeAsync` setar `_disposed=true` e `_lifetime.Token` de fato observar o cancelamento — o catch externo do loop só capturava `OperationCanceledException`, então essa exceção escapava, falhava `_heartbeatTask`, e o `catch (Exception ex)` genérico do próprio `Task.WhenAll(pending)` do `DisposeAsync` logava como erro inesperado um shutdown perfeitamente normal. **Corrigido:** `catch (ObjectDisposedException)` adicionado ao lado do `catch (OperationCanceledException)` já existente, mesmo tratamento "ignore: manager disposed". | `RingBufferManager.cs` (`RunHeartbeatAsync`'s outer catch) | Adicionar `catch (ObjectDisposedException)` ao lado do `catch (OperationCanceledException)` já existente no loop externo. |
| **F18** — **novo, achado ao escrever o teste do F17 (2026-08-21) — ✅ CORRIGIDO** | **Baixa-Média — confirmada em execução** | Lacuna residual própria do guard do F12/F15 (`catch (OperationCanceledException) when (!heartbeatWork.IsCompleted)`): se um callback rápido termina quase exatamente no mesmo instante em que `_lifetime` cancela, o guard pode avaliar `false` mesmo sendo um shutdown comum — cai no `catch (Exception ex)` genérico, que loga o `OperationCanceledException`/`TaskCanceledException` resultante como erro incondicional. Descartar o recurso nesse caminho ainda era seguro (o callback já tinha de fato terminado), só o nível de log estava errado — mesma classe do R15/F15/R17/O1. **Corrigido:** novo `catch (OperationCanceledException) when (_lifetime.IsCancellationRequested)` entre o guard do F12/F15 e o catch genérico, tratando como shutdown normal (`LogMessage`) em vez de erro. | `RingBufferManager.cs` (`RunHeartbeatAsync`, entre o catch do F12/F15 e o catch genérico) | Adicionar um catch dedicado para `OperationCanceledException` causada por `_lifetime`, entre o guard do F12/F15 e o catch genérico. |
| **F19** — **novo, Rodada 5 (2026-08-21) — ✅ CORRIGIDO** | **Alta — confirmada em execução (protocolo vermelho→verde completo)** | O `finally` do `DisposeAsync()`, no loop de drenagem dos itens ociosos restantes, usava um `foreach` bruto chamando `DisposeItemAsync` diretamente em cada item, em vez do helper já existente `DisposeItemsDefensivelyAsync` (já usado por `RemoveItemsAsync`). Um loop bruto aborta no primeiro item cujo `Dispose()` lança, vazando todo o restante mais `_lifetime`/`_meter`/`_activitySource` permanentemente (já que `_disposeGuard` torna uma segunda chamada a `DisposeAsync()` um no-op silencioso) — o mesmo modo de falha que a Rodada 1 já corrigiu uma vez (F5), só que para um gatilho diferente (o caminho de exceção do warmup). **Corrigido:** loop de drenagem roteado por `DisposeItemsDefensivelyAsync`. Confirmado **vermelho** (reversão temporária para o loop bruto: a `InvalidOperationException` do item propagou incondicionalmente de `DisposeAsync()`, batendo exatamente com o achado) → **verde** (helper restaurado). | `RingBufferManager.cs` (`DisposeAsync`'s `finally`, loop de drenagem) | Rotear o loop de drenagem por `DisposeItemsDefensivelyAsync` — implementado. Novo teste: `DisposeAsync_WhenAPooledItemsDisposeThrows_StillDisposesEveryOtherItem`. |
| **F20** — **novo, Rodada 5 (2026-08-21) — ✅ CORRIGIDO** | **Média-Alta — confirmada em execução (protocolo vermelho→verde completo)** | Com `BackgroundLogger(true)`, `DisposeAsync()` completava `_logQueue` e aguardava `_loggerTask` cedo demais — antes de vários dos seus próprios passos geradores de log mais tarde no método (o catch de falha do `Task.WhenAll(pending)`, a mensagem de timeout do grace period do F16, e o próprio log defensivo do loop de drenagem, agora via `DisposeItemsDefensivelyAsync`/F19). `LogMessage`/`LogError` só fazem `TryWrite` em `_logQueue` no modo background, sem fallback síncrono — então cada uma dessas chamadas era silenciosamente descartada uma vez a fila já completada. **Corrigido:** a completação de `_logQueue` e o await de `_loggerTask` foram deferidos para o fim do `finally` do `DisposeAsync`, depois de toda chamada de log possível acima já ter rodado. Confirmado **vermelho** (completação/await movidos de volta para o topo, `Assert.Contains` não encontrou a mensagem esperada) → **verde**. | `RingBufferManager.cs` (`DisposeAsync`) | Deferir a completação de `_logQueue`/await de `_loggerTask` para o fim do `finally` — implementado. Novo teste: `DisposeAsync_WithBackgroundLoggerEnabled_StillDeliversItsOwnLateLogMessages`. |
| **F21** — **novo, Rodada 5 (2026-08-21) — ✅ CORRIGIDO — escalado, você escolheu "Corrigir agora"** | **Baixa-Média — crescimento de memória, não um bug de correção; confirmada em execução (protocolo vermelho→verde completo)** | `_pendingHeartbeatDisposals` (o `ConcurrentBag<Task>` introduzido no F16 para rastrear disposals deferidos do callback de heartbeat) só era compactado pelo próprio `DisposeAsync()`, no fim da vida do buffer. Um callback de `HeartBeat` cronicamente mais lento que o próprio orçamento de pulso adicionava uma entrada nova a cada pulso expirado, por toda a vida do buffer — mesmo que quase todas essas entradas já tivessem terminado bem antes do próximo pulso expirar — crescendo sem limite em vez de rastrear só disposals genuinamente ainda em voo. Não é um bug de correção (`Task.WhenAll` sobre muitas tasks já completas mais uma pendente continua correto), mas é uma preocupação real de crescimento de memória para um processo de longa duração pareado com um recurso externo cronicamente lento/instável. **Escalado com duas opções (registrar para rodada futura vs. corrigir agora) — você escolheu corrigir agora.** Corrigido removendo entradas já concluídas da bag a cada novo `Add` — seguro sem sincronização adicional porque o próprio loop do `RunHeartbeatAsync` é o único escritor da bag (confirmado pelo comentário já existente do campo). Confirmado **vermelho** (reversão temporária para o `Add` sem prune; um callback sempre excedendo um pulso de 100ms por 6+ ciclos fez a bag crescer para exatamente 6 entradas) → **verde** (prune restaurado, bag ficou em ≤2 entradas). | `RingBufferManager.cs` (`RunHeartbeatAsync`'s deferred-dispose continuation) | Podar entradas já concluídas a cada novo `Add` — implementado. Novo teste: `PendingHeartbeatDisposals_UnderAChronicallySlowHeartbeat_DoesNotGrowUnbounded`. Ver [plano-de-acao.md](./plano-de-acao.md), Registro de execução, Rodada 5. |
| **F22** — **novo, varredura de código morto entre rodadas (2026-08-21) — ✅ REMOVIDO** | **Baixa — higiene, mesma classe do F13** | A limpeza do F13 levantou a pergunta de você: já que um guardrail defensivo pôde ficar morto depois que uma correção mais específica foi empilhada por cima, faz sentido uma varredura global por outros guardrails na mesma situação? Fiz essa varredura nos 16 arquivos de produção (2486 linhas). Achado: em `DisposeAsync()`, o `catch (OperationCanceledException)` em torno de `await Task.WhenAll(pending)` (`pending` = `_engineTask`/`_heartbeatTask`/`_sampleTickTask`) tinha ficado redundante — os três métodos por trás dessas tasks (`RunEngineAsync`, `RunHeartbeatAsync`, `RunSampleTickAsync`) já capturam `OperationCanceledException` no próprio nível externo deles (construído incrementalmente via R3/F3, F15, F17, F18, e a própria remoção do F13), então nenhum deles consegue mais deixar escapar essa exceção até este `WhenAll` — inclusive pelo caminho mais indireto considerado (um `ErrorHandler` do usuário lançando `OperationCanceledException` de dentro de um `LogError` interno a um desses três métodos), que já é absorvido pelo catch externo do próprio método antes de chegar aqui. O catch `Exception` já existente logo abaixo cobre o que sobra (é um supertipo). **Removido.** Verificado com um probe empírico antes de remover (o corpo do catch foi temporariamente substituído por um `throw` marcador; suíte completa net10.0 — 121/121 — rodou sem nunca atingi-lo, incluindo todo cenário de corrida com heartbeat/logger/warmup contra `DisposeAsync()`) e reconfirmado 121/121 depois da remoção real. Outros ~30 catches do arquivo e os demais 15 arquivos de produção foram revisados e descartados como candidatos — ver [`acompanhamento-convergencia.md`](./acompanhamento-convergencia.md) para o registro completo dos quase-candidatos descartados (a guarda de null/vazio do `AutoScaleDecision.Median`, o placeholder `RingBufferDefault.Capacity`, o catch de `RunLoggerAsync`). | `RingBufferManager.cs` (`DisposeAsync`, em torno de `Task.WhenAll(pending)`) | Removido — nenhuma recomendação pendente. |
| **F23** — **novo, Rodada 6 (2026-08-21) — ✅ CORRIGIDO — achado independentemente também pela Observabilidade (mesma causa raiz, mesma correção)** | **Alta — confirmada em execução (protocolo vermelho→verde completo)** | Nenhuma chamada a `Logger`/`ErrorHandler` (fornecidos pelo usuário via `OnError(...)`) estava protegida contra o próprio callback lançar exceção. No caminho síncrono (`BackgroundLogger(false)`, o padrão), especificamente no timeout do heartbeat: um `OnError` que lança dentro do catch F12/F15 de `RunHeartbeatAsync` (bem no `LogError(new TimeoutException("Timeout Heart Beat"))`) abortava aquele catch **antes** da linha seguinte, `_commands.Writer.TryWrite(EngineCommand.ReplaceOne())`, rodar — perdendo permanentemente um slot real do pool (o item preso nunca é substituído) enquanto `CurrentCapacity` continua reportando o número antigo, errado, para sempre, e falhando `_heartbeatTask`. Pior: quando o `Task.WhenAll(pending)` do próprio `DisposeAsync()` depois observa essa falha e chama seu próprio `LogError(ex)` para reportá-la, o mesmo `OnError` lança de novo — desta vez sem nada capturando, fazendo o próprio `DisposeAsync()` lançar ao chamador, violando diretamente o comentário de design do método ("must never throw"). No caminho `BackgroundLogger(true)`: um sink que lança mata `_loggerTask` silenciosamente na primeira mensagem ruim — toda mensagem seguinte pelo resto da vida da instância é enfileirada em `_logQueue` (ilimitada) mas nunca entregue, sem nenhuma exceção indicando que isso aconteceu. **Corrigido:** novo helper privado `SafeInvokeSink(Action invoke)` que envolve a chamada num try/catch e engole silenciosamente qualquer exceção (não há nada mais a logar sobre a falha — o próprio sink é o que está quebrado; estende a mesma filosofia "must never throw regardless of how a background pump ended" que o comentário do `DisposeAsync` já declara, e o mesmo princípio de wrap defensivo que `DisposeItemsDefensivelyAsync` já aplica aos itens do pool, F19). Aplicado em todo ponto de dispatch: as 3 chamadas síncronas dentro de `LogMessage`/`LogWarning`/`LogError`, e as 4 chamadas de dispatch dentro do loop de `RunLoggerAsync`. Confirmado **vermelho** em 2 testes novos (reversão temporária de `SafeInvokeSink` para chamadas diretas; o teste de heartbeat falhou com "Expected the replacement item to be acquirable despite the throwing OnError handler", o de background logger falhou com "Expected at least 3 OnError invocations..., got 1") → **verde** (fix restaurado, os dois passam). | `RingBufferManager.cs` (`LogMessage`, `LogWarning`, `LogError`, `RunLoggerAsync`) | Novo helper `SafeInvokeSink` envolvendo toda invocação de `Logger`/`ErrorHandler` — implementado. Novos testes: `HeartBeatTimeout_WithAThrowingOnErrorHandler_StillReplacesTheStuckItemAndDisposesCleanly`, `BackgroundLogger_WhenOnErrorThrowsOnce_StillDeliversLaterMessages`. |

**Nota sobre um achado que quase virou correção incorreta (Rodada 6, Estabilidade):** o mesmo passe também caracterizou como "contrato de exceção inconsistente" o fato de `WarmupAsync`/`BuildWarmupAsync`/`SwitchToAsync` deixarem escapar a exceção crua da factory quando o lote falha por completo (zero progresso), mas empacotarem em `InvalidOperationException("RingBuffer did not reach initial capacity")` quando há progresso parcial antes da falha. Isso foi escalado ao mantenedor, que inicialmente escolheu "sempre empacotar em `InvalidOperationException`" para um contrato consistente. Antes de implementar, uma verificação da suíte de testes já existente revelou dois testes passando (`WarmupAsync_WhenFactoryThrows_PropagatesTheRealException_AndDoesNotHang` e `SwitchToAsync_WhenFactoryThrowsDuringScaleUp_PropagatesRealException_AndEngineSurvives`, ambos em `RingBufferContractTests.cs`) cujo próprio nome e comentário afirmam que deixar escapar a exceção real no caso zero-progresso é comportamento **deliberado**, estabelecido desde a Rodada 1/P0#1 (o próprio código comenta: "Surface the real factory failure to WarmupAsync's caller instead of the generic 'did not reach initial capacity'"). Essa evidência nova foi levada de volta ao mantenedor, que revisou a decisão: **manter o comportamento exatamente como está** (nenhuma mudança de código) e corrigir só a frase do R20 em `usage-rabbitmq.md:45` que afirmava incondicionalmente que `InvalidOperationException` "ainda é lançada" — a frase foi reescrita para não nomear um tipo específico, mais um parêntese explicando a assimetria por design (ver R20 em §6). Nenhum ID novo foi atribuído a este item — nada estava de fato errado, e nada mudou de comportamento; registrado aqui como nota de metodologia porque é exatamente o tipo de correção que a auditoria evitou fazer ao verificar a evidência antes de agir, não depois.

**Nota sobre cobertura de testes:** todo teste de idempotência de dispose na suíte (`RingBufferValueTests.cs:61`, teste de contrato 1.4) testa double-dispose apenas *sequencial*. F2 — o que de fato corrompe o estado do pool — precisa de double-dispose *concorrente* e é invisível para a suíte atual. Nenhum teste em lugar nenhum usa uma factory que lança exceção.

---

## 6. Achados detalhados — Resiliência (comportamento sob falha)

**Veredito do passe: NÃO PRONTO como documentado.** O `doc/guides/usage-rabbitmq.md` promete explicitamente degradação graciosa em caso de perda de conexão; a implementação atual não entrega isso, e o problema não é exclusivo de elástico — reproduz também em buffers `FixedCapacity` simples.

| ID | Severidade | Achado | Localização | Recomendação |
|---|---|---|---|---|
| **R1** | **Crítica — confirmada em execução — ✅ CORRIGIDO** | Mesmo defeito de F1, alcançado por uma falha externa realista (RabbitMQ lança exceção em vez de cancelar). Reproduzido: travamento na inicialização sem exceção/log/métrica quando a factory lança exceção durante o warmup (não limitado por `AcquireTimeout`, que ainda não está em vigor); perda permanente do plano de gestão após uma falha de factory pós-warmup — confirmado que o autoscaler nunca se recupera mesmo depois que a factory volta a ficar saudável; perda silenciosa de capacidade via `Invalidate()` (2 de 3 itens obteníveis enquanto `CurrentCapacity` ainda reporta 3). | `RingBufferManager.cs:513,516,528,363,370,152,158,303-304,382` | Ação 1 (§3). **Corrigido — ver [plano-de-acao.md](./plano-de-acao.md) P0#1.** |
| **R2** | **Alta — confirmada em execução — ✅ CORRIGIDO** | `DisposeAsync` ou trava para sempre (se o warmup nunca completou) ou lança a exceção antiga da factory *antes* de drenar itens do pool e descartar `_lifetime`/`_meter`/`_activitySource` — o shutdown falha e vaza ao mesmo tempo. | `RingBufferManager.cs:251-285` | Ação 5 (§3); limitar com timeout o await de `_warmup.Value` também. **Corrigido pela combinação de P0#2 (o await de `_warmup.Value` já não trava, pois `WarmupCoreAsync` internamente está limitado por `_lifetime.Token`) + P0#5 (limpeza incondicional) — ver [plano-de-acao.md](./plano-de-acao.md).** |
| **R3** | **Alta — confirmada em execução — ✅ CORRIGIDO** | Um callback de `HeartBeat` que bloqueia para a bomba para sempre (o token do `Task.Run` não cancela um delegate em execução) e prende um item do pool permanentemente, com a guarda de timeout pretendida sendo inalcançável. Probe de 4 segundos: 1 invocação em vez de ~7 esperadas, item permanentemente ausente do pool. | `RingBufferManager.cs:623-631,616` | Ação 4 (§3); decidir o que acontece com o item preso (provavelmente `Invalidate()`). **Corrigido com `Invalidate()` no timeout — ver [plano-de-acao.md](./plano-de-acao.md) P0#4.** |
| **R4** | **Alta — confirmada em execução — ✅ CORRIGIDO** | O autoscale-on-fault nunca faz scale-up quando `initialCapacity == minCapacity` — uma configuração legal e natural ("começar no piso, crescer sob pressão"). Confirmado: 5 timeouts de acquire consecutivos, capacidade nunca se moveu, com uma factory perfeitamente saudável. Nenhum sample ou exemplo de guia atinge isso hoje, mas é silencioso e total quando se aplica. | `RingBufferManager.cs:403` | Basear a decisão em "próxima capacidade estritamente maior que a atual", não em uma comparação de igualdade de valor; validar em tempo de `Build`. **Corrigido:** `next = CurrentCapacity < Capacity ? Capacity : MaxCapacity` garante `next > CurrentCapacity` em todo caso alcançável — a validação em `Build` sugerida como alternativa deixou de ser necessária. Ver [plano-de-acao.md](./plano-de-acao.md) P1#9. |
| **R5** | **Média — confirmada em execução, atinge o sample publicado — ✅ CORRIGIDO** | O prazo do scale-up é a *janela de amostragem* (`SamplesBase`), que também precisa cobrir `delta × latência da factory por item`. O `RingBufferPlusRabbitSample` publicado (`delta=10`, janela de 5s) tolera só 500ms/item — o probe confirma que ele abre 10 canais reais do broker e descarta todos por timeout, ganhando zero capacidade, repetindo a cada ciclo de falha (carga extra sobre um broker já em dificuldade). | `RingBufferManager.cs:504,510,530-535`; `samples/RingBufferPlusRabbitSample/Program.cs:73` | Desacoplar o prazo da janela de amostragem ou validar a relação em `Build`; manter itens parcialmente criados em vez de descartar todos; redimensionar o sample. **Corrigido:** prazo agora é `quantity × FactoryTimeout`; itens parciais são mantidos e `_currentCapacity` avança pela quantidade real obtida. **O sample RabbitMQ não precisou ser redimensionado** — com `FactoryTimeout` padrão (15s) e `delta` de até 10, o novo prazo (até 150s) já resolve a configuração publicada sem alterá-la. Ver [plano-de-acao.md](./plano-de-acao.md) P1#8. |
| **R6** ✅ CORRIGIDO | **Média** | O loop do engine processa comandos estritamente em série; um scale-down lento o bloqueia por até `SamplesBase` (30s padrão), período em que `Fault`/`ReplaceOne`/`Switch` não são atendidos — o scale-up por autoscale fica "surdo" por até 30s exatamente no momento em que a carga pode estar subindo de novo. **Corrigido:** o valor de 30s nunca foi calibrado para o v5 (herdado do v4 sem alteração — confirmado via `git show v.4.0.1`; nenhum ADR/benchmark o avaliou). `RemoveItemsAsync` agora é oportunista: só remove o que já está ocioso agora (`TryRead` não-bloqueante), nunca espera itens ocupados liberarem. Redução parcial avança `_currentCapacity` pela quantidade real removida (mesmo padrão do scale-up parcial, R5/P1#8) — sem isso, uma redução parcial deixaria `_currentCapacity` "mentindo" sobre a capacidade real. Ver `plano-de-acao.md` P3#R6. | `RingBufferManager.cs:357-361,548,543` | Não bloquear o loop de comandos por disponibilidade de itens; tornar o scale-down oportunista em vez de esperar por uma quantidade fixa. |
| **R7** | **Média — parcialmente corrigido** | O orçamento de falhas reseta *antes* de uma tentativa de scale-up rodar, então um scale-up que depois falha (timeout, R5) ainda queima o orçamento — o buffer precisa acumular um ciclo de falhas inteiro novo antes de tentar de novo, agravando a degradação exatamente quando não deveria. O contador também nunca decai com o tempo (só reseta em um scale-up disparado), então falhas isoladas e não relacionadas, espaçadas por dias, ainda podem disparar um scale-up muito depois que a carga que as causou já passou — ver também F7. **Corrigido:** o reset só acontece após um `MoveToCapacityAsync` que retorna `true` (sucesso completo) — uma tentativa falha/parcial não queima o orçamento, então a próxima falta única já tenta de novo. **Não corrigido, deliberadamente (Opção A escolhida):** o contador ainda não decai com o tempo — falhas antigas e não relacionadas ainda podem, em teoria, somar até o limiar. Sem incidente real confirmado; deixado no backlog caso vire um problema de fato. Ver `plano-de-acao.md` P3#F7/R7. | `RingBufferManager.cs:399-405` | Resetar o orçamento só após um movimento *bem-sucedido*; fazer o contador decair ao longo de uma janela de tempo ligada a `SamplesBase`. |
| **R8** | **Média — ✅ CORRIGIDO** | Operações de scale que falharam/foram desfeitas são registradas de forma idêntica às bem-sucedidas em métricas (`scale.operations`, `scale.duration`) e traces (sem `ActivityStatusCode.Error`). Durante uma instabilidade, um dashboard lê "escalou 40 vezes, saudável" quando a verdade é 40 falhas consecutivas e zero mudança de capacidade — exatamente o sinal errado no momento exato em que um operador precisa do sinal certo. | `RingBufferManager.cs:452-455,484-497` | Ação 7 (§3). **Corrigido:** tag `success` (bool) adicionada às duas métricas + `ActivityStatusCode.Ok`/`Error` no span. `usage-observability.md` atualizado para não ficar desatualizado por essa própria correção. Ver [plano-de-acao.md](./plano-de-acao.md) P1#7. |
| **R9** ✅ CORRIGIDO | **Baixa** | `ScaleDownMin` calculado, documentado (com fórmula específica na doc XML), e nunca lido em tempo de execução. Mesmo defeito de Estabilidade F11, visto pelo lado da documentação. **Corrigido junto com F11:** parágrafo "at minimum capacity" removido da doc XML de `AutoScaleAcquireFault`; `src/docs` regenerado. | `Commands/IRingBufferElasticBuilder.cs`; `RingBufferBuilder.cs:284-290` | Remover a configuração morta e seu parágrafo de doc, ou implementá-la. |
| **R10 ✅ CORRIGIDO** | **Baixa — confirmada em execução** | O limiar de falhas documentado está com off-by-one (ver §4.3), e o guia de RabbitMQ/autoscale nomeia o tipo de exceção errado para falhas de validação em tempo de `Build` (`IndexOutOfRangeException` documentado, `InvalidOperationException` de fato lançado — confirmado por teste). **Corrigido nas duas partes:** tipo de exceção via U-21; limiar via Decisão C (`>=` no código). | `RingBufferManager.cs:400`; `RingBufferBuilder.cs:268-279` | Ver §4.3 para o limiar; correção de uma palavra para o tipo de exceção (também Usabilidade U-21). |
| **R11** ✅ CORRIGIDO | **Baixa** | A própria bomba de heartbeat da biblioteca chama `AcquireAsync` internamente; sob carga legítima sustentada, o timeout do próprio heartbeat contribui para o orçamento de falhas do autoscale — um scale-up autoinfligido disparado pelo health check interno do pool, não pela demanda de consumidores. **Corrigido (Opção A — isentar):** `AcquireAsync` público extraído para um núcleo compartilhado (`AcquireCoreAsync`) com um parâmetro `countsTowardFaultBudget`; o heartbeat agora chama `AcquireForHeartbeatAsync` (`countsTowardFaultBudget: false`) — seu próprio timeout não posta mais `Fault()`. Demanda externa genuína continua contando normalmente. Ver `plano-de-acao.md` P3#R11. | `RingBufferManager.cs:615,170-181` | Isentar acquires de heartbeat do contador de falhas, ou marcá-los separadamente nas métricas. |
| **R12** | **Baixa — suspeita, não reproduzida — ✅ CORRIGIDO** | Um item devolvido via `TurnbackAsync` concorrentemente com o dispose pode ser silenciosamente descartado sem ser disposed (janela estreita; não reproduzida no probing). Mesmo caminho de código de F5. | `RingBufferManager.cs:337,277-281,345` | Coberto pela Ação 5 (§3). **Corrigido junto com F5 — mesmo código, mesma correção — ver [plano-de-acao.md](./plano-de-acao.md) P0#5.** |
| **R13** — **novo achado, registrado ao corrigir R6 (2026-08-20) — ✅ FECHADO POR DECISÃO (2026-08-21, arquitetura mantida)** | **Média** | O mesmo mecanismo do R6 existe do lado do scale-up: `CreateItemsAsync` chama a `Factory` sequencialmente dentro do loop único do engine, até `quantity × FactoryTimeout` (correção do R5) — durante esse tempo, o engine também não atende `Fault`/`Switch`/`ReplaceOne`. Diferente do R6, aqui não há atalho "oportunista" trivial: os itens precisam ser criados de verdade via `Factory`, não há nada já disponível para pegar sem esperar. Corrigir isso de verdade exigiria mover as chamadas de `Factory` para fora do loop único do engine (uma tarefa em background escrevendo em `_currentCapacity`/`_availableItems`), o que reabre a invariante central do [ADR001](../doc/adr/ADR001V02-concurrency-model-for-ring-buffer-manager-scale-up-and-down.md) ("todo estado mutável de escala pertence só ao consumidor único"). **Mudança arquitetural fora de escopo desta rodada — decisão confirmada.** **Endereçado pelo lado possível sem tocar arquitetura:** o único "botão" disponível hoje é `FactoryTimeout` (o teto é `quantity × FactoryTimeout`) — documentado explicitamente na doc XML de `Factory(...)` (4 interfaces) e no guia de autoscale, deixando claro que o default de 15s é herdado do v4 sem calibração para o v5 (confirmado via `git show v.4.0.1`) e que o valor deve ser calibrado pelo usuário para a latência real da própria factory. **Considerada e descartada:** tornar `timeout` obrigatório (sem default) — quebraria 50 dos 53 call sites deste repositório e é inconsistente com o resto da API fluente (todo outro parâmetro de timeout segue o padrão opcional-com-default). **Considerada e descartada:** baixar o default para 5s — pesquisa (ver `plano-de-acao.md` P3#R13) mostrou que cenários reais de cold start (Lambda→RDS com IAM, documentado pela AWS) levam ~6s, ou seja, 5s ficaria **abaixo** de um cenário legítimo documentado; 15s mantém margem confortável. Valor do default mantido inalterado. **Reavaliado na Rodada 2 (2026-08-20):** ainda corretamente caracterizado, decisão de manter fora de escopo continua razoável (nenhuma mudança desta rodada tocou a invariante do ADR001). Dois refinamentos: (1) o R14 (novo, abaixo) muda o formato prático do pior caso — uma factory degradada agora falha *rápido* (aborta no 1º item ruim) em vez de bloquear pelo teto inteiro, então o tempo de bloqueio do engine tende a ser menor na prática do que o R13 documenta como pior caso — mas a troca é que o lote não ganha nada nesse cenário (ver R14), então o custo migra de "engine bloqueado por muito tempo" para "o mesmo ciclo de falha se repete rápido e sem produzir nada", o que não é necessariamente menos grave. (2) `CreateSingleReplacementAsync` é uma instância do mesmo mecanismo do R13 (chama `Factory` dentro do loop único do engine), não citada na discussão original do R13 embora já estivesse no escopo do P0#1 — vale incluir se o R13 for revisitado (magnitude menor: `1 × FactoryTimeout`, não `quantity ×`). **Reavaliado na Rodada 3 (Resiliência), quantificado:** probe medindo o tempo de bloqueio real do engine com falhas por timeout (não exceção rápida) confirmou uma amplificação linear exata: `(maxConsecutiveFactoryFailures + 1) × FactoryTimeout` — 316ms com tolerância 0, 624ms com tolerância 1, 1242ms com tolerância 3 (`FactoryTimeout=300ms`). Não é um bug novo — é o R14 (tolerar falhas) combinado com o R13 (já fora de escopo arquitetural), mas nem a doc XML de `maxConsecutiveFactoryFailures` nem o guia de autoscale mencionavam essa multiplicação. **Endereçado (doc, sem mudança de código, decisão confirmada):** nota adicionada na doc XML de `Factory(...)` (4 interfaces) e no guia `usage-elastic-autoscale.md` explicando a fórmula. **Fechado por decisão do mantenedor (2026-08-21, limpeza de backlog entre rodadas):** esta é a 3ª confirmação da mesma decisão (Rodadas 1 e 2 já a haviam confirmado) — deixa de constar como item "parcialmente endereçado"/pendente. A limitação arquitetural em si permanece (é inerente ao ADR001, não um bug), mas a decisão de não mudá-la e mitigar só por documentação é final, não um TODO em aberto. | `RingBufferManager.cs` (`CreateItemsAsync`, chamado de dentro de `MoveToCapacityAsync`) | Decisão final — nenhuma ação pendente. Reabrir só se uma futura rodada trouxer evidência nova que mude o trade-off (ex.: um incidente real de produção causado por este bloqueio). |
| **R14** — **novo, Rodada 2 (2026-08-20) — ✅ CORRIGIDO** | **Média-Alta — confirmada em execução** | `CreateItemsAsync` aborta o lote inteiro de scale-up na primeira falha/timeout de item (`throw;` propaga para fora do `while`, capturado pelo catch externo que só devolve o que já tinha sido criado ANTES da falha) — mesmo que o orçamento (`quantity × FactoryTimeout`) ainda tenha sobra para tentar os itens restantes. Probe (`quantity=4`, `FactoryTimeout=2s`→orçamento de 8s, só o item #3 artificialmente lento em 5s, os outros ~20ms): lote abortado depois de só 1 item tentado, ~2s de um orçamento de 8s, `CurrentCapacity` não mudou. Se o item que falha for o primeiro do lote, zero capacidade é ganha mesmo que os outros 3 teriam sucesso instantâneo. Combinado com o R7 (retry imediato após falha), esse padrão pode se repetir a cada ciclo de fault sem nunca progredir. **Corrigido, com um trade-off discutido e resolvido:** "tentar todos os itens sempre" (Opção A original) foi descartada depois de identificar que interage com o R13 (bloqueio do engine) — várias falhas no lote aumentariam o tempo de bloqueio no pior caso. Solução final: novo parâmetro opcional `Factory(value, timeout, maxConsecutiveFactoryFailures = 0)` — `0` (default) preserva o fail-fast de hoje (zero mudança de comportamento sem opt-in); `N > 0` tolera até N falhas *consecutivas* (reseta a cada sucesso) antes de desistir do resto do lote. Sem trade-off residual: quem não pedir a tolerância não perde nada, quem pedir sabe exatamente o custo (pode bloquear o engine por mais tempo em troca de mais capacidade real). Ver `plano-de-acao.md` P4#R14. **Reavaliado na Rodada 3 (Resiliência), confirmado correto:** 6 configurações de probe (tolerância 0/1/2/3, falhas consecutivas no limite exato, falhas espalhadas com recuperação) todas bateram com a semântica documentada — inclusive a nuance de que uma falha tolerada ainda perde aquele slot (o lote é por-tentativa, não por-sucesso). | `RingBufferManager.cs:632-697` (`CreateItemsAsync`) | Continuar tentando os itens restantes do lote até o orçamento (`quantity × FactoryTimeout`) de fato esgotar, em vez de parar na primeira falha. |
| **R15** — **novo, Rodada 2 (2026-08-20) — ✅ CORRIGIDO** | **Baixa-Média — confirmada em execução (2 probes)** | O catch externo de `CreateItemsAsync` (`OperationCanceledException`, sem `when`) e o catch de `CreateSingleReplacementAsync` logam incondicionalmente como `TimeoutException`, sem distinguir "o prazo da factory de fato esgotou" de "`DisposeAsync` cancelou `_lifetime` enquanto isso estava em andamento". Probe 1 (scale-up): dispose no meio de um scale-up, bem dentro dos orçamentos de 2s/item e 20s×4 geral, logou `Timeout ScaleUp 0/4` mesmo sem nada ter de fato expirado. Probe 3 (replacement, via heartbeat→`Invalidate()`→`ReplaceOne`): mesmo padrão, `Timeout factory (replacement)` num shutdown normal. Um shutdown que corre com um scale/replacement em andamento sempre produz um log de erro que parece problema de factory/broker, quando é só um encerramento limpo. **Corrigido:** ambos os catches agora checam o token do chamador (`token`/`_lifetime`, não o CTS de timeout por item) antes de decidir — cancelamento de shutdown vira `LogMessage` (nível debug/informativo), só um timeout real continua gerando `LogError` com `TimeoutException`. Ver `plano-de-acao.md` P4#R15. **Reavaliado na Rodada 3 (Resiliência), confirmado correto nos 2 locais originais**, mas **2 achados irmãos novos, mesma classe, locais diferentes:** o `RunHeartbeatAsync` tinha um caso residual não cobertos pelo F12/R15 (ver **F15**, corrigido na mesma rodada), e o `WarmupCoreAsync` tinha exatamente a mesma ambiguidade nunca tocada por esta correção (ver **R17**, novo, corrigido na mesma rodada). | `RingBufferManager.cs:665-676` (`CreateItemsAsync`), `:745-748` (`CreateSingleReplacementAsync`) | Checar `token.IsCancellationRequested` (o token do chamador/`_lifetime`, não só o CTS de timeout por item) antes de logar como timeout; logar em nível mais baixo ou nem logar quando é de fato um cancelamento de lifetime. **Reavaliado na Rodada 4 (Resiliência), confirmado correto, sem regressão.** |
| **R16** — **novo, Rodada 2, reavaliado e reproduzido na Rodada 3 (2026-08-20) — ✅ CORRIGIDO (mudança arquitetural)** | **Média (era Baixa/não-reproduzida) — reproduzida em execução** | `AutoScaleDecision.EvaluateScaleDown` só disparava quando `isInitCapacity` ou `isMaxCapacity` era exatamente verdadeiro (igualdade estrita). Como scale-up (R5) e scale-down (R6) preservam progresso parcial, `CurrentCapacity` podia parar estritamente entre dois níveis, de onde nenhuma das duas condições nunca mais batia — o scale-down automático nunca mais reavaliaria redução a partir desse ponto, prendendo a capacidade silenciosamente acima do necessário. Não reproduzido na Rodada 2 (6 configurações de probe, 0 ocorrências). **Reproduzido na Rodada 3 (Resiliência)** via um cenário mais realista: `ElasticCapacity(4,2,10)` + `AutoScaleAcquireFault(1)` com `maxConsecutiveFactoryFailures=1` e um padrão de falha controlado — o scale-up tolerou falhas o bastante para não lançar, mas só completou 3 dos 6 itens pedidos, pousando em **7** (estritamente entre `Capacity=4` e `MaxCapacity=10`). Com o pool inteiro ocioso por 4 janelas de amostra completas (3.2s), a capacidade nunca saiu de 7. **Achado importante:** o R14 (tolerância a falhas) não criou essa lacuna — ela existe desde o R5/P1#8 — mas o propósito do R14 (tolerar uma factory com "instabilidades recuperáveis ocasionais") é exatamente o cenário mais propenso a produzir pousos parciais fora de tier repetidamente, então habilitar `maxConsecutiveFactoryFailures>0` (o uso documentado e pretendido) torna o R16 *mais* provável na prática, não menos. **Corrigido (mudança arquitetural confirmada com você, não só extensão de elegibilidade):** a correção em duas partes — (1) `EvaluateScaleDown` agora avalia por faixa (`currentCapacity > capacity` / `currentCapacity <= capacity`), não só nas duas igualdades exatas; (2) a margem de segurança do limiar deixou de ser um valor absoluto fixo calculado uma vez a partir de `Min/Init/MaxCapacity` (`maxCapacity - capacity + 2`) e passou a ser escalada por `currentCapacity` (`currentCapacity - capacity + 2`) — a fórmula original é reproduzida exatamente quando `currentCapacity` é igual a `capacity`/`maxCapacity`, mas agora é sempre alcançável a partir de qualquer posição fora de tier (a fórmula fixa original seria matematicamente inalcançável no cenário de 7 exato reproduzido: pediria mais itens ociosos do que os 7 que existem). A parte (2) foi discutida e confirmada com você antes de implementar — a alternativa mais conservadora (só a parte 1) não resolvia o cenário exato reproduzido. `ScaleDownInit`/`ScaleDownMax` (armazenados no builder/manager) foram removidos — a decisão agora é pura em função de `currentCapacity`/`minCapacity`/`capacity`, sem estado pré-computado. | `AutoScaleDecision.cs` (`EvaluateScaleDown`); `RingBufferManager.cs` (`ProcessTickAsync`, `RingBufferBuilder.cs` (`_scaledownInit`/`_scaledownMax`, removidos) | ~~Baixa prioridade dada a dificuldade de reprodução~~ — corrigida: banda de elegibilidade + margem escalada por `currentCapacity`. **Reavaliado na Rodada 4 (Resiliência) com um probe mais longo (4 ciclos sucessivos de fault→scale-up→ocioso→scale-down), confirmado sem regressão — o buffer nunca ficou preso em nenhuma das várias posições fora de tier produzidas.** Ver também R18/R19 (Rodada 4) para o caso degenerado que a fórmula da margem ainda tinha, agora também corrigido. |
| **R17** — **novo, Rodada 3 (2026-08-20) — ✅ CORRIGIDO** | **Média — confirmada em execução (probe + teste de contrato)** | Mesma classe do R15 (ambiguidade "cancelamento genuíno vs. shutdown normal"), em um local que o R15 não tocou: `WarmupCoreAsync` captura `OperationCanceledException` ao aguardar a conclusão do comando `Warmup` e sempre define `reached = false` — inclusive quando a causa é um `DisposeAsync()` concorrente cancelando `_lifetime`, não uma falha genuína de alcançar a capacidade. O `if (!reached)` seguinte sempre loga `LogError` com `InvalidOperationException("RingBuffer did not reach initial capacity")`, incondicionalmente. Um segundo caminho para o mesmo `reached = false` existe no próprio loop do engine (`RunEngineAsync`/`ProcessCommandAsync`, ramo `Warmup`): ao capturar `OperationCanceledException` ali, ele resolve `cmd.Completion?.TrySetResult(false)` — ou seja, `WarmupCoreAsync` nem sempre vê a exceção diretamente; o cancelamento também chega como um `reached = false` "normal", sem exceção. Reproduzido com um probe/teste de contrato: `WarmupAsync()` com uma factory lenta, seguido de `DisposeAsync()` concorrente, loga o `InvalidOperationException` como erro em toda execução, mesmo sendo um encerramento limpo. **Corrigido:** o `if (!reached)` agora checa `_lifetime.IsCancellationRequested` (cobrindo os dois caminhos — o catch local e o `TrySetResult(false)` do engine) antes de decidir `LogMessage` (shutdown normal) vs. `LogError` (falha genuína) — a exceção lançada ao chamador continua a mesma nos dois casos (o warmup genuinamente não completou), só o nível de log muda. | `RingBufferManager.cs` (`WarmupCoreAsync`, `RunEngineAsync`/`ProcessCommandAsync` ramo `Warmup`) | Checar `_lifetime.IsCancellationRequested` antes de logar como falha genuína de capacidade — mesmo padrão do R15, aplicado a este local. **Reavaliado na Rodada 4 (Resiliência), confirmado correto, sem regressão** — só um comentário de código adjacente estava impreciso (dizia que `MoveToCapacityAsync` "lança" `OperationCanceledException`, quando na verdade `CreateItemsAsync` absorve o cancelamento internamente e retorna uma contagem parcial/zero — corrigido o texto do comentário, sem mudança de comportamento. |
| **R18** — **novo, achado ao revisar a correção do R16 (2026-08-21) — reproduzido e ✅ CORRIGIDO na Rodada 4** | **Baixa→Média (reavaliada), depois corrigida** | A margem de segurança escalada por `currentCapacity` que o R16 introduziu (`currentCapacity - capacity + 2` para o ramo acima da capacidade inicial) tem um caso degenerado: quando a capacidade inicial é **2** (o valor mínimo legal — `ElasticCapacity(2, 2, max, ...)` passa a validação de `Build`), a margem se reduz a `currentCapacity - 2 + 2 = currentCapacity`. A mediana das amostras de ociosos nunca pode superar `currentCapacity` (não existem mais itens ociosos do que o total de itens), então a condição `median > currentCapacity` nunca é satisfeita — scale-down a partir de qualquer posição acima da capacidade inicial fica inalcançável, **inclusive exatamente na capacidade máxima**. Confirmado que **não é uma regressão do R16**: a fórmula fixa original (`maxCapacity - capacity + 2`) tinha exatamente o mesmo problema quando `capacity = 2` (`maxCapacity - 2 + 2 = maxCapacity`, igualmente inalcançável). Reproduzido na Rodada 4 (Resiliência) por sweep exaustivo determinístico de `EvaluateScaleDown` (1.573 combinações de `(min, capacity, max)`), confirmando `capacity == 2` como o **único** valor degenerado do ramo superior. **Decisão do mantenedor (2026-08-21, revisada):** inicialmente registrado sem correção nesta rodada (mesmo padrão do F13/F14); depois que o R19 (achado espelhado no ramo inferior, abaixo) tornou os dois lados do mesmo defeito visíveis juntos, o mantenedor decidiu **corrigir a causa raiz agora**. **Corrigido:** ambos os limiares (superior e inferior) agora são limitados (`Math.Min(..., currentCapacity - 1)`), garantindo que a mediana sempre possa, em princípio, superá-los — a mudança só afeta configurações com `capacity == 2`/`minCapacity == 2`; qualquer outra configuração já tinha o limiar original abaixo desse teto, sem mudança de comportamento. Documentado na emenda 2026-08-21 (Rodada 4) do [ADR003V02](../doc/adr/ADR003V02-median-sample-autoscaling-algorithm.md). | `AutoScaleDecision.cs` (`EvaluateScaleDown`) | Limiar limitado a `currentCapacity - 1` em ambos os ramos — implementado. |
| **R19** — **novo, espelho do R18 no ramo inferior (Rodada 4, Resiliência, 2026-08-21) — ✅ CORRIGIDO** | **Média-Alta — confirmada em execução (sweep exaustivo determinístico)** | Mesma causa raiz do R18, ramo inferior: quando a capacidade mínima é **2**, a margem `currentCapacity - minCapacity + 2` também se reduz a `currentCapacity` — mas como esse ramo usa `>=` (não o `>` estrito do ramo superior), a condição não é literalmente impossível, só exige que a mediana seja exatamente igual a `currentCapacity`, ou seja, **zero acquisições em toda a janela de amostragem** — uma barra materialmente mais estrita do que qualquer outro valor de capacidade mínima exige. Um `HeartBeat` configurado (que faz seu próprio acquire periódico) pode tornar essa janela de zero-acquisições praticamente inatingível sob operação real contínua, não só sob tráfego zero contrivado. Confirmado por sweep exaustivo (mesmo do R18): `minCapacity == 2` é o único valor que produz essa exigência. **Corrigido junto com o R18** (mesma mudança — limiar limitado a `currentCapacity - 1`): agora tolera 1 item ativo em vez de exigir ociosidade total. | `AutoScaleDecision.cs` (`EvaluateScaleDown`, ramo abaixo/na capacidade inicial) | Mesma correção do R18 — implementado. |
| **R20** — **novo, Rodada 5 (2026-08-21) — ✅ CORRIGIDO (doc)** | **Baixa** | `doc/guides/usage-rabbitmq.md:45` era ambíguo/enganoso sobre o que `maxConsecutiveFactoryFailures` (R14) realmente faz — ele **não** ajuda `WarmupAsync`/`BuildWarmupAsync` a ter sucesso; essa tolerância só se aplica a chamadas de factory em regime estacionário (scale-up/replacement), não ao preenchimento inicial do warmup. **Corrigido:** linha reescrita para declarar isso explicitamente. | `doc/guides/usage-rabbitmq.md:45` | Reescrever a linha — implementado. |
| **R21** — **novo, Rodada 5 (2026-08-21) — ✅ CORRIGIDO (doc)** | **Baixa** | `doc/adr/ADR003V02-median-sample-autoscaling-algorithm.md:47` afirmava que um "geralmente" valia incondicionalmente para a redução à fórmula original — mas a própria emenda da Rodada 4 ao mesmo ADR (o limiar limitado a `currentCapacity - 1`, R18/R19) capa essa mesma margem, então essa redução só vale quando o teto não entra em ação (`capacity`/`minCapacity != 2`). **Corrigido:** parêntese qualificador adicionado à linha. Ver também a nota no fim de §5 (Estabilidade) sobre a frase logo acima nesta mesma linha do guia RabbitMQ (R20), reavaliada na Rodada 6. | `doc/adr/ADR003V02-median-sample-autoscaling-algorithm.md:47` | Adicionar o parêntese qualificador — implementado. |
| **R22** — **novo, Rodada 6 ("O7-residual", 2026-08-21) — ✅ CORRIGIDO** | **Média — confirmada em execução (protocolo vermelho→verde completo)** | A correção do O7 (Rodada 5) propagou `HadGenuineFailure` a partir do **retorno** de `CreateItemsAsync` — mas esse método tem um segundo caminho de saída: quando o lote inteiro faz zero progresso, ele em vez disso **lança** o próprio `lastFailure` diretamente (`if (created.Count == 0 && lastFailure is not null) throw lastFailure;`), pulando o retorno da tupla inteiramente. Nesse caminho, `hadGenuineFailure` em `MoveToCapacityAsync` ficava no seu default `false`, reabrindo exatamente o mesmo mascaramento que o O7 corrigiu — só que alcançável pelo throw, não pelo return (ex.: uma falha genuína na 1ª tentativa do lote, tolerada via `maxConsecutiveFactoryFailures`, seguida de uma 2ª tentativa cancelada por um `DisposeAsync()` comum antes de fazer qualquer progresso). **Corrigido:** a chamada a `CreateItemsAsync` dentro do ramo de scale-up de `MoveToCapacityAsync` agora é envolvida num `try/catch (Exception) { hadGenuineFailure = true; throw; }` — seguro e precisamente escopado, porque toda exceção capaz de escapar de `CreateItemsAsync` é, por construção, o `throw lastFailure` de falha genuína explícito (os caminhos de cancelamento comum dentro de `CreateItemsAsync` sempre retornam normalmente, nunca lançam). Confirmado **vermelho** (reversão temporária do try/catch; o teste falhou no `Assert.Contains` — a métrica mostrou o estado mascarado `cancelled=true`) → **verde**. | `RingBufferManager.cs` (`MoveToCapacityAsync`'s ramo de scale-up) | `try/catch` envolvendo a chamada a `CreateItemsAsync`, setando `hadGenuineFailure` antes de relançar — implementado. Novo teste: `ScaleUp_WithAZeroProgressGenuineFailureThenRacedByDisposeAsync_StillReportsTheFailure_NotJustCancelled` (em `RingBufferObservabilityTests.cs`, ao lado do teste original do O7). |

---

## 7. Achados detalhados — Usabilidade (superfície de API e documentação)

**Veredito do passe: PRONTO COM RESSALVAS.** A profundidade da documentação está genuinamente acima da média para uma biblioteca deste porte — 10 guias orientados a tarefas, ADRs registrando o raciocínio, uma referência gerada por membro, zero links quebrados em 100 arquivos markdown, e todos os 5 samples compilam limpos contra a superfície v5 real. O problema está concentrado no punhado de coisas que um adotante novo toca primeiro.

### Crítica

| ID | Achado | Localização |
|---|---|---|
| **U-01 ✅ CORRIGIDO** | O Quickstart do README usa uma variável `cancellation` não declarada — confirmado que falha na compilação como impresso (`CS0103` ×2). Este também é o readme do pacote NuGet, ou seja, o primeiro código que qualquer visitante do nuget.org vê. **Corrigido:** adicionada `CancellationToken cancellation = default;`; confirmado que compila e executa (probe verbatim). | `README.md:36-53` |
| **U-02 ✅ CORRIGIDO** | O guia de DI instrui a injetar `IRingBufferManualScaleService<T>` diretamente — confirmado que lança `InvalidOperationException` na resolução, porque `AddRingBuffer` só registra `IRingBufferService<T>`. O próprio sample do repositório faz deliberadamente o oposto (pattern-matching) e cita o ADR007 como motivo. **Corrigido:** removida a instrução de injeção direta; texto agora diz que só `IRingBufferService<T>` é registrado e orienta o pattern-match (igual ao sample), com a correção adicional de que é o `SwitchToAsync` que lança, não o cast/pattern-match em si (que sempre sucede, pois `RingBufferManager<T>` sempre implementa `IRingBufferManualScaleService<T>` — confirmado em `RingBufferBuilder.cs:130,158,179` todos chamando o mesmo `BuildCore`). | `doc/guides/usage-dependency-injection.md:46,54`; contradito por `samples/RingBufferPlusApiSample/Controllers/WeatherForecastController.cs:49-56` |

### Alta

| ID | Achado | Localização |
|---|---|---|
| **U-03 ✅ CORRIGIDO** | A descrição do alvo de scale-up no guia de autoscale é ambígua, e uma leitura natural é o inverso exato do comportamento real. **Corrigido:** texto agora descreve a fórmula corrigida no P1#9 (`Capacity` se abaixo de `Capacity`, senão `MaxCapacity`) sem ambiguidade. | `doc/guides/usage-elastic-autoscale.md:29` |
| **U-04 ✅ CORRIGIDO** | O contrato de retorno documentado de `SwitchToAsync` é o comportamento v4 literal (uma rejeição de "já em andamento" que o próprio ADR007 cita como o defeito que removeu). **Corrigido:** doc XML agora descreve o comportamento real (fila, não rejeição; `bool` reflete alvo alcançado só com `LockWhenScaling`); `src/docs` regenerado. | `Commands/IRingBufferManualScaleService.cs:23-26` |
| **U-05 ✅ CORRIGIDO** | "Retorna assim que agendado... isso acontece rápido e sempre" subestima que um `SwitchToAsync` atrás de um scale em andamento pode bloquear ~30s independentemente de `LockWhenScaling`. **Corrigido** nos dois guias — agora nomeiam o limite real (`baseTimer`, 30s padrão, verificado em `RingBufferDefault.cs:36`). | `doc/guides/usage-lock-when-scaling.md:32`; `usage-elastic-manual-scale.md:35` |
| **U-06 ✅ CORRIGIDO** | O segundo cenário do sample RabbitMQ é um no-op (`LockWhenScaling` não tem efeito no builder de autoscale) enquanto seu comentário afirma demonstrar a mudança de comportamento de lock da v5, usando texto do v4. **Corrigido:** segundo cenário removido do sample (decisão sua) — a demonstração real de `LockWhenScaling` já existe em `RingBufferPlusBasicManualScale`. | `samples/RingBufferPlusRabbitSample/Program.cs:87,93-94` |
| **U-07 ✅ CORRIGIDO** | Limiar de falhas do autoscale documentado como off-by-one em **quatro** locais separados, incluindo a seção do CHANGELOG posicionada como única referência de migração. Ver §4.3. **Corrigido:** Decisão C escolheu corrigir o código (`>=`) para bater com a documentação existente — nenhuma das 4 doc precisou mudar. | `Commands/IRingBufferElasticBuilder.cs:98`; referência gerada; `usage-elastic-autoscale.md:29,37`; `CHANGELOG.md:19` |
| **U-08 ✅ CORRIGIDO** | A "única referência de migração" do CHANGELOG omite as duas quebras v4→v5 com maior chance de atingir código real: `SwitchToAsync` removido inteiramente de `IRingBufferService<T>`, e as três interfaces de builder renomeadas sem nenhum mapeamento antigo→novo. **Corrigido:** dois novos bullets adicionados ao CHANGELOG — mapeamento `IRingBuffer<T>`/`IRingBufferBuild<T>`/`IRingBufferScaleCapacity<T>` → v5, e a nota explícita sobre `SwitchToAsync`. Nomes v4 reverificados via `git show v.4.0.1:src/RingBufferPlus/Commands/*.cs` (não apenas confiando na auditoria original). | `CHANGELOG.md:16` vs. `git diff v.4.0.1` real |
| **U-21 ✅ CORRIGIDO** | O guia de autoscale nomeia o tipo de exceção errado para falhas de validação em tempo de `Build`. **Corrigido:** `IndexOutOfRangeException` → `InvalidOperationException`, confirmado contra `RingBufferBuilder.cs:270,276`. | `doc/guides/usage-elastic-autoscale.md:42` |
| **U-30** — **novo, Rodada 4 (2026-08-21) — ✅ CORRIGIDO** | `doc/guides/usage-lock-when-scaling.md:28` afirma que `IRingBufferAutoScaleBuilder<T>` "also exposes the [`LockWhenScaling`] method... it is a documented no-op there" — desatualizado para o `develop` atual: `LockWhenScaling(bool)` foi **removido inteiramente** de `IRingBufferAutoScaleBuilder<T>` nesta mesma rodada de hardening (`CHANGELOG.md`, [ADR007V02](../doc/adr/ADR007V02-redesign-of-the-public-fluent-api-surface.md)) — confirmado ausente de `Commands/IRingBufferAutoScaleBuilder.cs`. O guia contradiz diretamente o próprio CHANGELOG do projeto: onde o leitor esperaria um no-op inofensivo, agora ocorre um erro de compilação. **Corrigido:** o parágrafo agora descreve a remoção e que encadear `.LockWhenScaling()` depois de `.AutoScaleAcquireFault()` é erro de compilação. | `doc/guides/usage-lock-when-scaling.md:28` |
| **U-31** — **novo, Rodada 4 (2026-08-21) — ✅ CORRIGIDO** | Duas afirmações falsas relacionadas, ambas datando desta mesma rodada de hardening anterior: (1) a doc `<returns>` de `SwitchToAsync` (`Commands/IRingBufferManualScaleService.cs:23-28` — que **renderiza**, e o U-04 já tinha revisado sem notar isto) diz que um resultado `false` com `LockWhenScaling` significa que a operação "**was undone on its own timeout**" — falso: [ADR010V01](../doc/adr/ADR010V01-allow-partial-capacity-gains-when-a-scale-up-cannot-fully-complete.md) e o teste `SwitchToAsync_WhenFactoryFailsPartwayThroughScaleUp_KeepsPartialCapacity` confirmam que nada é desfeito — a capacidade parcial ganha é mantida. (2) `usage-lock-when-scaling.md` (linhas 7, 24, 32, 42) e `usage-elastic-manual-scale.md:35` repetem a mesma confusão E afirmam que `baseTimer` (30s) é o timeout da própria operação de escala — falso: o prazo real de scale-up é `quantity × FactoryTimeout` (comentário explícito em `RingBufferManager.cs:649-653`); scale-down não tem timeout algum; `baseTimer`/`numberSamples` só configuram a cadência de amostragem do scale-down automático. Confirmado pelo teste `SwitchToAsync_ScaleUpNeedingMoreTimeThanSamplesBase_StillSucceeds`. Bônus: um `<para>` mal formado (fora de `<summary>`/`<remarks>`) em `IRingBufferBuilder.cs:92-94` continha a mesma afirmação errada, mas nunca renderizava (texto morto, não visível no IntelliSense). **Corrigido:** doc XML de `SwitchToAsync` reescrita; as duas frases dos dois guias reescritas; o `<para>` malformado envolvido num `<remarks>` correto. | `Commands/IRingBufferManualScaleService.cs:23-28`; `doc/guides/usage-lock-when-scaling.md:7,24,32,42`; `usage-elastic-manual-scale.md:35`; `IRingBufferBuilder.cs:92-94` |

### Média

| ID | Achado | Localização |
|---|---|---|
| **U-09** ✅ CORRIGIDO | A doc XML de `AcquireAsync` não menciona nenhuma exceção, mas ele lança `OperationCanceledException`/`ObjectDisposedException` em caminhos para os quais o guia de DI ativamente encaminha as pessoas (tokens de request-abort). **Corrigido:** doc XML reescrita com `<remarks>`/`<exception>` explicando os dois casos, mais a propagação da falha de warmup cacheada (ADR011). | `Commands/IRingBufferService.cs:54-60`; `RingBufferManager.cs:151,189-201` |
| **U-10** ✅ CORRIGIDO | `LockWhenScaling()` é um método público, visível no IntelliSense, no builder de autoscale, que é um no-op documentado — ver §4.1. **Corrigido:** a Decisão A removeu o método inteiramente de `IRingBufferAutoScaleBuilder<T>` (confirmado: não existe mais no arquivo) — a armadilha deixou de existir, em vez de só ficar melhor documentada. Ver [ADR007V02](../doc/adr/ADR007V02-redesign-of-the-public-fluent-api-surface.md) e `plano-de-acao.md` P2 Decisão A. | `Commands/IRingBufferAutoScaleBuilder.cs:65-73` |
| **U-11** ✅ CORRIGIDO | 12 operações distintas do builder são publicadas como 35 declarações em 4 interfaces com docs XML copiadas à mão (um contribuinte direto para a inconsistência de U-07) e 35 páginas de referência gerada sem nenhuma sinalização entre elas. Não é uma violação do ADR — o esboço do ADR era hedged — mas o formato publicado e sua justificativa não estão documentados. **Corrigido:** nota adicionada em `concepts.md` explicando que a duplicação é deliberada — cada interface devolve seu próprio tipo para manter a cadeia fluente dentro do modo já escolhido (ver ADR007V02). | `IRingBufferBuilder.cs`, `IRingBufferFixedBuilder.cs`, `IRingBufferElasticBuilder.cs`, `IRingBufferAutoScaleBuilder.cs` |
| **U-12** ✅ CORRIGIDO | Nenhum exemplo de código em lugar nenhum para `OnError` (verifiquei todos os guias, README, todos os 5 samples); `Invalidate()` — a preocupação central do mundo real para um pool de conexão/canal — recebe uma frase e dois call sites de sample não explicados. Ambos são exatamente o que um adotante precisa quando a produção dá errado. **Corrigido:** exemplo mínimo de `usage-heartbeat.md` ganhou `.OnError(...)` e uma chamada a `Invalidate()` explicada dentro do callback; os dois call sites de sample (`WeatherForecastController.cs`, `RingBufferPlusBasicSample/Program.cs`) ganharam comentários explicando o propósito. | — |
| **U-13** ✅ CORRIGIDO | O timeout padrão de 30 segundos para escala (`baseTimer`) nunca é nomeado onde importa (o guia de lock-when-scaling); no modo de escala manual, `numberSamples` é validado em `Build` mas não tem nenhum efeito em runtime (a bomba de amostragem nunca inicia sem autoscale). **Corrigido:** a nomeação do `baseTimer` já tinha sido corrigida no U-05; a parte do `numberSamples` sem efeito em modo manual foi documentada agora em `usage-elastic-manual-scale.md`. | `doc/guides/usage-lock-when-scaling.md:32,37`; `RingBufferManager.cs:325-327`; `RingBufferBuilder.cs:268-279` |
| **U-14** ✅ CORRIGIDO | Duas registrações de DI do mesmo `T` sob nomes diferentes resolvem silenciosamente para a última registrada, em injeção de construtor simples — confirmado ao rodar. Não coberto pelo aviso do guia de DI sobre mesmo-nome/mesmo-`T`. **Corrigido:** nota adicionada em `usage-dependency-injection.md` explicando que `AddRingBuffer<T>` sempre registra como `IRingBufferService<T>` (o nome só importa para `WarmupRingBufferAsync`), com a alternativa (`IEnumerable<IRingBufferService<T>>` + busca por nome). | `HostingExtensions.cs:33` |
| **U-22** ✅ CORRIGIDO | Um `WarmupAsync` que falha destrói permanentemente a instância (exceção cacheada) sem nenhuma menção na doc XML pública, enquanto todo guia recomenda registro como singleton no DI. Ver §4.2. **Corrigido:** `WarmupAsync()` agora retenta a partir do zero quando a tentativa anterior falhou (CAS sobre um novo `Lazy<Task>`); `AcquireAsync`/`SwitchToAsync` deliberadamente não retentam sozinhos. Ver [ADR011V01](../doc/adr/ADR011V01-retry-path-for-a-failed-warmup-async-instead-of-a-permanently-broken-instance.md) e `plano-de-acao.md` P2 Decisão B. | `RingBufferManager.cs:136`; `Commands/IRingBufferService.cs:62-71` |
| **U-25** — **novo, Rodada 2 (2026-08-20) — ✅ CORRIGIDO** | Confirmado: `usage-elastic-autoscale.md:29` ainda diz "once the counter **exceeds** `numberOfFaults`" — a mesma confusão off-by-one que a Decisão C (P2) e o U-07 deveriam ter eliminado em todo lugar. A doc XML de `AutoScaleAcquireFault` já diz "reached" corretamente; esta frase do guia foi reescrita para o U-03 quando o código ainda usava `>` (e "exceeds" era literalmente verdade), e nunca foi revisitada depois que a Decisão C trocou para `>=`. **Corrigido:** "exceeds" → "reaches". | `doc/guides/usage-elastic-autoscale.md:29` |
| **F-U30** — **novo, Rodada 3 (2026-08-20) — ✅ CORRIGIDO** | `usage-heartbeat.md:43` ainda dizia "...invokes your callback... and then returns the item to the pool itself once the callback returns **or times out**" — errado para o ramo de timeout desde a correção do F12 (commit `942d53d`): no timeout o item **não** é devolvido ao pool; `ReplaceOne` é enfileirado (descarta-e-substitui) e o objeto físico só é disposed depois, dentro da continuação `heartbeatWork.ContinueWith`, quando o callback órfão de fato termina. A descrição correta já existia no próprio `CHANGELOG.md:38`, mas nunca foi carregada para o guia didático. **Corrigido:** "What happens internally" reescrito para distinguir o caminho normal (devolvido ao pool, sujeito a `Invalidate()`) do caminho de timeout (slot substituído imediatamente, disposal físico deferido); "Trade-offs" ganhou a mesma nota resumida. | `doc/guides/usage-heartbeat.md:43,52` |

### Baixa

| ID | Achado | Localização |
|---|---|---|
| U-15 ✅ CORRIGIDO | O parêntese da doc XML de `ElasticCapacity` está aritmeticamente errado (30s/100 amostras ≠ 100ms; são 300ms, corretamente afirmado uma linha acima). **Corrigido:** "100ms" → "300ms". | `IRingBufferBuilder.cs:82` |
| U-16 ✅ CORRIGIDO | O "Default true" da doc de `LockWhenScaling` lê como o padrão da configuração; é o padrão do parâmetro — a configuração é `false` a menos que o método seja chamado. **Corrigido:** doc reescrita para distinguir "default do parâmetro" de "default da configuração". | `IRingBufferElasticBuilder.cs:69` |
| U-17 ✅ CORRIGIDO | O comportamento do contador de falhas na capacidade máxima não está documentado (mesmo código de Estabilidade F7). **Corrigido junto com o próprio F7:** doc XML de `AutoScaleAcquireFault` ganhou um parágrafo explicando o reset em `MaxCapacity` e o reset só após sucesso (R7). | `RingBufferManager.cs:399-405` |
| U-18 ✅ CORRIGIDO (documentado como limitação aceita) | A referência gerada rotula errado um link de namespace (rótulo vs. alvo não batem; o link em si resolve normalmente). **Corrigido:** é uma limitação do gerador (`XmlDocMarkdownGenerator`), não corrigível editando o arquivo gerado nem mudando o namespace de `HostingExtensions` (deliberado, convenção .NET). Documentada explicitamente em `CONTRIBUTING.md` como limitação conhecida/aceita. | `src/docs/assemblies/Microsoft.Extensions.DependencyInjection/HostingExtensions.md:22` |
| U-19 ✅ CORRIGIDO | O "O que há de novo" do README descreve a v5.0.0 como puras breaking changes; a única funcionalidade aditiva (observabilidade nativa) não é mencionada nessa seção. **Corrigido:** frase adicionada mencionando a observabilidade nativa (ADR008) como aditiva, sem breaking change. | `README.md:20-24` |
| U-20 ✅ CORRIGIDO | `CONTRIBUTING.md` nunca declara comandos de build/teste nem que `src/docs/**` precisa ser regenerado após mudanças na doc XML — diretamente relevante dado que U-07/U-15 estão duplicados nessa árvore gerada. **Corrigido:** seção "Unit tests" ganhou os comandos (`dotnet build`/`dotnet test`); nova seção "Regenerating the generated API reference" explica o passo (incluindo o cuidado de rodar o `.exe` do próprio `bin/`, não via `dotnet run`) e documenta a limitação do U-18. | `CONTRIBUTING.md:78-90` |
| U-23 ✅ CORRIGIDO | As duas sobrecargas de `RingBuffer<T>.New` discordam sobre nullability do mesmo argumento lógico. **Corrigido (código, não só doc):** a segunda sobrecarga era anotada `string?` mas lança `ArgumentNullException` se `null` — a assinatura prometia algo que o comportamento não entrega. Anotação trocada para `string` (não-nullable), batendo com a primeira sobrecarga e com o comportamento real. | `RingBufferExtension.cs:24,39` |
| U-24 ✅ CORRIGIDO | `RingBufferDefault.Capacity = 2` é documentado como "a capacidade padrão", mas é inalcançável — todo caminho até `Build`/`BuildWarmupAsync` exige definir a capacidade explicitamente. **Corrigido (doc, não remoção):** confirmado que o campo é de fato usado internamente (placeholder antes de `FixedCapacity`/`ElasticCapacity`), mas nunca observável no serviço construído. Removê-lo seria uma breaking change de API pública sem justificativa forte o suficiente para esta severidade (Baixa) — doc XML reescrita para descrever com precisão o que o campo realmente é. | `RingBufferDefault.cs:38-41`; `RingBufferBuilder.cs:101-117` |
| **U-26** — **novo, Rodada 2 (baixa-média) — ✅ CORRIGIDO** | `usage-lock-when-scaling.md:32` (e, com menor severidade, `concepts.md:18,60`) linkam `ADR001V01` especificamente ao explicar `LockWhenScaling`/`AcquireAsync` — mas a V01 (linha 36) ainda tem a afirmação defasada que o F8 apontou ("`LockWhenScaling` já documenta esse trade-off"), exatamente a que a `ADR001V02` foi criada para corrigir/anotar como errada. `usage-elastic-autoscale.md:31` já linka a V02 corretamente para um ponto semelhante — os guias estão inconsistentes entre si. **Corrigido:** as 3 referências (`usage-lock-when-scaling.md`, `concepts.md` ×2) trocadas para `ADR001V02`. | `doc/guides/usage-lock-when-scaling.md:32`; `doc/guides/concepts.md:18,60` |
| **U-27** — **novo, Rodada 2 — ✅ CORRIGIDO** | `CONTRIBUTING.md:74,79` (seção "API stability policy") linka `ADR004V01`, mas a `ADR004V02` é a versão atual/Aceita e documenta a segunda exceção de versionamento (a remoção do `LockWhenScaling` sem major bump) mais o aviso explícito de que "um terceiro pedido do tipo deve ser recebido com escrutínio real". Um contribuidor lendo o CONTRIBUTING para a política atual não fica sabendo que uma segunda exceção já aconteceu. **Corrigido:** as 2 referências trocadas para `ADR004V02`. | `CONTRIBUTING.md:74,79` |
| **U-28** — **novo, Rodada 2 — ✅ CORRIGIDO** | `RingBufferExtension.cs:24-46`: as duas sobrecargas de `RingBuffer<T>.New` lançam `ArgumentNullException` se `buffername` for `null` (confirmado no código), mas nenhuma das duas doc XML tem uma tag `<exception>` — diferente de `AddRingBuffer`, `WarmupRingBufferAsync`, `WarmupAsync`, `AcquireAsync`, `Build`, `BuildWarmupAsync`, que todas documentam suas exceções. A referência gerada correspondente (`RingBuffer-1/New.md`) também não tem seção "Exceptions". **Corrigido:** `<exception cref="ArgumentNullException">` adicionada nas duas sobrecargas; `src/docs` regenerado. | `RingBufferExtension.cs:24-46` |
| **U-29** — **novo, Rodada 2 — ✅ CORRIGIDO** | `HostingExtensions.cs:28,52`: a doc XML de `AddRingBuffer`/`WarmupRingBufferAsync` descreve a condição do `ArgumentNullException` como "Buffer name null or empty" — mas a implementação (`ArgumentNullException.ThrowIfNull(buffername)`) só guarda contra `null`; uma string vazia `""` é aceita silenciosamente. Não estava nos 24 itens originais; baixo impacto real, mas é uma divergência doc/código verificada. **Corrigido:** doc XML reescrita para descrever a validação real (só `null`; vazio é aceito); a de `WarmupRingBufferAsync` também menciona o caso "buffer não encontrado". | `HostingExtensions.cs:28,52` |
| **F-U31** — **novo, Rodada 3 (baixa-média) — ✅ CORRIGIDO** | 4 links de guia ainda apontavam para `ADR007V01` (superada pela `ADR007V02`, que adicionou a emenda de remoção do `LockWhenScaling`), mesma classe do U-26/U-27 já fechados — o `concepts.md:55` era inclusive inconsistente com a própria linha 52 do mesmo arquivo, que já linkava a V02. Busca (`grep -rn "ADR007"`) confirmou nenhuma outra referência solta fora dessas quatro (mais o CHANGELOG histórico, corretamente deixado como está). **Corrigido nas 4 (`concepts.md:55`, `usage-dependency-injection.md:54`, `usage-elastic-autoscale.md:25`, `usage-elastic-manual-scale.md:31`) e, ao verificar `doc/architecture/overview.md` (fora do escopo de busca original), mais 2 achadas na mesma varredura: `ADR007V01`→V02 e `ADR001V01`→V02 (mesma classe do U-26, incluindo a mudança de slug de arquivo).** | `doc/guides/concepts.md:55`; `usage-dependency-injection.md:54`; `usage-elastic-autoscale.md:25`; `usage-elastic-manual-scale.md:31`; `doc/architecture/overview.md:44-45` |
| **F-U32** — **novo, Rodada 3 (baixa-média) — ✅ CORRIGIDO** | `samples/RingBufferPlusBasicTriggerScale/Program.cs`: comentário `//AcquireAsync fault` posicionado errado nos dois blocos de acquire concorrente. Linha 62 precedia o 3º acquire (`buffer3`) num bloco de exatamente 3 concorrentes com capacidade inicial 3 — nenhum dos três deveria falhar ali, o comentário estava simplesmente errado. Linha 83 precedia `buffer3` de novo num bloco de 4, mas o fault real (dado capacidade 3, `AcquireTimeout` 500ms) ocorre em `buffer4` (linha 86), uma posição depois. **Corrigido:** comentário errado da linha 62 removido; comentário da linha 83 movido para imediatamente antes de `buffer4` (linha real que demonstra o fault que este sample existe para disparar). | `samples/RingBufferPlusBasicTriggerScale/Program.cs:62,83,86` |
| **F-U33** — **novo, Rodada 3 (baixa) — ✅ CORRIGIDO** | `maxConsecutiveFactoryFailures` (R14) tem doc XML consistente e correta nas 4 interfaces de builder, e o `CHANGELOG.md` documenta o parâmetro — mas nenhum guia em `doc/guides/*.md` o menciona; `usage-elastic-autoscale.md` e `usage-rabbitmq.md` são os dois guias onde "factory com instabilidades recuperáveis ocasionais" (a própria descrição da doc XML) é mais relevante, e nenhum dos dois mencionava a opção nas suas seções de trade-offs. Não é um erro de correção, só um gap de descoberta — um leitor não tem como achar esse knob fora da referência de API/CHANGELOG. **Corrigido:** `usage-elastic-autoscale.md` já ganhou a menção como parte da nota de documentação R13×R14 (ver R13); `usage-rabbitmq.md` ganhou um novo bullet em "Trade-offs / limitations" mencionando a opção no contexto de falhas transitórias de criação de canal. | `doc/guides/usage-elastic-autoscale.md`; `doc/guides/usage-rabbitmq.md` |
| **F-U34** — **novo, Rodada 3 (baixa) — ✅ CORRIGIDO** | Nenhum guia (nem `usage-observability.md`, que cobre só `Meter`/`ActivitySource`, nem `usage-background-logger.md`, nem nenhuma doc XML) descreve que um scale-up/replacement cancelado por um shutdown normal agora loga informativamente (`LogMessage`, correção do R15) enquanto um timeout genuíno continua logando `LogError` com `TimeoutException`. Não é regressão (nada afirmava o contrário antes) — é um gap: quem monitora `LogError`/`TimeoutException` como sinal de saúde de factory/broker não tem como saber que essa distinção existe, e por que um erro antes visto pode desaparecer num restart limpo. **Corrigido:** nova seção "Logging: a normal shutdown is not the same signal as a genuine timeout" adicionada em `usage-observability.md`, explicando a distinção e linkando os guias de heartbeat/background-logger. | `doc/guides/usage-observability.md` |
| **U-32** — **novo, Rodada 4 (baixa-média) — ✅ CORRIGIDO** | Doc XML incompleta em 3 pontos: `IRingBufferService<T>.WarmupAsync` documenta `InvalidOperationException` mas não `ObjectDisposedException` (que `RingBufferManager.WarmupAsync` de fato lança), inconsistente com o `AcquireAsync` vizinho na mesma interface, que documenta os dois; `IRingBufferManualScaleService<T>.SwitchToAsync` não tinha nenhuma tag `<exception>`, apesar de dois caminhos reais de exceção (`InvalidOperationException` no escape-hatch, `ObjectDisposedException`); `HostingExtensions.WarmupRingBufferAsync` documenta seu próprio `ArgumentNullException` mas não o `InvalidOperationException` que se propaga do `WarmupAsync()` interno. **Corrigido:** as 3 tags `<exception>` faltantes adicionadas. | `Commands/IRingBufferService.cs`; `Commands/IRingBufferManualScaleService.cs`; `HostingExtensions.cs` |
| **U-33** — **novo, Rodada 4 (baixa-média) — ✅ CORRIGIDO** | A seção "Logging" do `usage-observability.md` (adicionada na Rodada 3, F-U34) só nomeava o caso de ScaleUp/replacement — faltavam os casos de Warmup (R17) e Heartbeat (F15/F18), corrigidos na mesma Rodada 4. A tabela de métricas também não avisava que `scale.operations`/`scale.duration`'s `success=false` também não distinguia shutdown de falha genuína (ver O1) antes da correção. **Corrigido:** seção "Logging" reescrita citando os 3 casos; tabela de métricas atualizada com a nova tag `cancelled` (ver O1/O2). | `doc/guides/usage-observability.md` |
| **U-34** — **novo, Rodada 4 (baixa-média) — ✅ CORRIGIDO** | `usage-observability.md` menciona que o acquire do `HeartBeat` conta para `acquire.duration`/`acquire.faults`, mas não que esse acquire é deliberadamente isento de disparar `AutoScaleAcquireFault` (R11) — quem observa `acquire.faults` para prever reação de autoscale pode ser enganado num buffer com `HeartBeat` + `AutoScaleAcquireFault` configurados juntos. **Corrigido:** frase adicionada explicando a isenção. | `doc/guides/usage-observability.md` |
| **U-35** — **novo, Rodada 4 (baixa-média) — ✅ CORRIGIDO (resolvido pela correção do R18/R19)** | O caso degenerado do R18 (`initialCapacity == 2` tornando scale-down inalcançável) não era descoberto em lugar nenhum que um usuário configurando `ElasticCapacity(2, ...)` fosse ler — só no relatório de auditoria. Achado antes do R18 ser corrigido nesta mesma rodada; a doc foi atualizada já refletindo a correção (não a limitação), então este item ficou automaticamente resolvido pela correção do R18/R19, não por uma doc separada sobre uma limitação que deixou de existir. | `Commands/IRingBufferElasticBuilder.cs`; `doc/guides/usage-elastic-autoscale.md` |
| **U-36** — **novo, Rodada 4 (baixa)** | A doc XML de `AutoScaleAcquireFault` dava as duas fórmulas de limiar sem declarar os comparadores (`>` estrito no ramo superior, `>=` no inferior — uma assimetria real e testada, `AutoScaleDecisionTests.cs:63-72,96-104`), e o guia usava a palavra "reaches" para os dois, implicando inclusivo em ambos. **Corrigido:** a doc XML reescrita durante a correção do R18/R19 já declara "exceeds" (ramo superior) vs. "reaches or exceeds" (ramo inferior) explicitamente. | `Commands/IRingBufferElasticBuilder.cs` |
| **F-U37** — **novo, Rodada 5 (baixa-média) — ✅ CORRIGIDO** | A seção "What happens internally" de `usage-heartbeat.md` não mencionava que `DisposeAsync()` pode esperar (limitado por `PulseHeartBeat`, correção do F16 na Rodada 4) pelo disposal deferido do recurso de um callback de heartbeat órfão. **Corrigido:** frase adicionada nessa seção, mais um novo bullet em "Common errors" sobre essa espera limitada por pulso e sua implicação no orçamento de shutdown/timeout de um orquestrador de host. A mesma nota irradiou, pelo mesmo fato, para `concepts.md` (bullet de Dispose) e `usage-dependency-injection.md` (parágrafo de disposal, com nota sobre a interação com `HostOptions.ShutdownTimeout`). | `doc/guides/usage-heartbeat.md`; `doc/guides/concepts.md`; `doc/guides/usage-dependency-injection.md` |
| **F-U38** — **novo, Rodada 5 (baixa) — ✅ CORRIGIDO (fundido com Observabilidade O9 — mesmo achado, mesma correção)** | A seção "Logging" de `usage-observability.md` (adicionada na Rodada 3 para o F-U34) não documentava a mensagem de timeout do grace period introduzida pela correção do F16 na Rodada 4 (`"DisposeAsync did not wait for {N} orphaned heartbeat callback(s)..."`). **Corrigido:** 4º bullet adicionado à seção. | `doc/guides/usage-observability.md` |
| **F-U39** — **novo, Rodada 6 (2026-08-21) — ✅ CORRIGIDO (doc)** | **Média** | A mesma seção "Logging" de `usage-observability.md` afirmava que a mensagem de grace-period-timeout do F16 loga "at the same informational level" das três mensagens de desambiguação shutdown-vs-falha listadas logo acima — mas o próprio O8 (Rodada 5), corrigido no mesmo commit que introduziu essa frase, escalou deliberadamente essa mensagem de `LogMessage` (Debug) para `LogWarning`, justamente porque é um caso diferente (um vazamento real de duração indeterminada, não uma ambiguidade shutdown-vs-falha). O texto nunca foi atualizado para refletir a própria mudança de código+CHANGELOG feita naquele commit. **Corrigido:** frase reescrita dizendo que loga em `LogWarning`, "a step above the informational level of the three cases above", com a razão explicada. | `doc/guides/usage-observability.md:62` |

**Verificado e considerado correto** (o que delimita a lista de correções — isto *não* são achados): todos os 7 trechos de código dos guias compilam contra a superfície v5 real; `concepts.md` e `usage-observability.md` são totalmente precisos frente ao código; `usage-heartbeat.md` e `usage-fixed-capacity.md` batem exatamente com a implementação; a exclusividade em tempo de compilação para a qual o ADR007 foi escrito (nenhum `SwitchToAsync` no escopo após `AutoScaleAcquireFault`) de fato funciona; os requisitos de processo do ADR004 (suporte só para 5.x, política de `[Obsolete]`-antes-de-remover) estão ambos satisfeitos em `SECURITY.md`/`CONTRIBUTING.md`; zero links relativos ou de âncora quebrados em todos os 100 arquivos markdown; a referência de API gerada está sincronizada com a doc XML atual; todos os cinco samples compilam e, juntos, cobrem fixo+heartbeat, ambas as variantes de escala manual, autoscale, DI no ASP.NET Core, e RabbitMQ.

---

## 8. Achados detalhados — Observabilidade (pilar novo a partir da Rodada 4)

**Veredito do passe: PRONTO, com refinamentos aplicados.** Este é o primeiro passe dedicado deste pilar (antes, achados de observabilidade só apareciam de raspão pelo passe de Resiliência — R8 na Rodada 1, R15 na Rodada 2). O nível de log já estava limpo (nenhuma nova instância do padrão R15/F15/R17 foi encontrada nos `LogError`/`LogMessage` de `RingBufferManager.cs` — todos os pontos plausíveis já checavam o token relevante). Os achados novos ficaram na camada de métricas/tracing, nunca auditada por esse ângulo antes.

| ID | Severidade | Achado | Localização | Recomendação |
|---|---|---|---|---|
| **O1** — **novo, Rodada 4 (2026-08-21) — ✅ CORRIGIDO** | **Média — confirmada em execução** | `scale.operations`/`scale.duration` e a atividade `"RingBufferPlus.Scale"` reportavam um scale cancelado por um `DisposeAsync()` comum de forma idêntica a uma falha real de factory — nenhuma tag distinguia os dois casos, mesmo padrão do R15/F15/R17 (shutdown confundido com falha genuína), só que uma camada acima (métricas/traces, não logs). Confirmado com um probe: `SwitchToAsync` racing `DisposeAsync()` gravava `success=false` e `ActivityStatusCode.Error`, indistinguível de uma falha real de factory. **Corrigido:** nova tag `cancelled` (bool) em `scale.operations`/`scale.duration` e na atividade; `ActivityStatusCode` só é `Error` quando `!ok && !cancelled` (falha genuína) — `Ok` em sucesso ou quando cancelado por shutdown. | `RingBufferManager.cs` (`MoveToCapacityAsync`'s `finally`) | Tag `cancelled` distinguindo shutdown de falha genuína, espelhando o padrão já existente na atividade de Acquire — implementado. |
| **O2** — **novo, Rodada 4 (2026-08-21) — ✅ CORRIGIDO** | **Baixa-Média — confirmada em execução** | `AcquireCoreAsync` nunca chamava `activity?.SetStatus(...)` em nenhum caminho (sucesso, timeout genuíno, ou cancelamento por shutdown/chamador) — diferente da atividade de Scale, que já define status. Um backend de tracing que filtra/agrega por status de span nunca veria nenhum resultado de `AcquireAsync` como span de erro, mesmo num timeout genuíno. **Corrigido:** `ActivityStatusCode.Ok` no sucesso; `Error` só quando `timedOut` é verdadeiro (timeout genuíno); `Ok` para cancelamento por shutdown ou pelo próprio chamador (não é um problema de saúde do buffer). | `RingBufferManager.cs` (`AcquireCoreAsync`) | Definir `ActivityStatusCode` nos 3 caminhos de saída, espelhando a distinção já feita para logs — implementado. |
| **O4** — **novo, Rodada 4 (2026-08-21) — resolvido como efeito colateral da correção do R18** | **Baixa** | Um buffer preso fora de tier (pelo R18, antes de corrigido) não tinha nenhum sinal de observabilidade dedicado — `EvaluateScaleDown` retornando `null` indefinidamente não gera métrica, log, nem trace algum; o único sinal disponível era o gauge `capacity.current` simplesmente nunca diminuir, indistinguível de "este buffer legitimamente ainda está ocupado". Não era um bug de correção nova, só uma faceta de observabilidade do R18 já registrado. **Resolvido:** como o R18 (e seu espelho R19) foram corrigidos nesta mesma rodada, o cenário que motivou este achado não existe mais na forma reproduzida — não ficou nenhuma ação de observabilidade pendente. | `AutoScaleDecision.cs` | Nenhuma ação adicional necessária após a correção do R18/R19. |
| **O5** — **novo, Rodada 4 (2026-08-21) — ✅ CORRIGIDO** | **Baixa** | A seção "Logging" do `usage-observability.md` (adicionada na Rodada 3 para o F-U34) só cobria o caso de ScaleUp/replacement, omitindo Warmup (R17) e Heartbeat (F15/F18) — os mesmos mecanismos, em locais diferentes. Fundido com o achado equivalente da Usabilidade (U-33) e corrigido junto. | `doc/guides/usage-observability.md` | Ver U-33. |
| **O6** — **novo, Rodada 5 (2026-08-21) — ✅ CORRIGIDO (sem ciclo vermelho→verde completo — ver nota)** | **Baixa-Média — mecânico/aditivo** | `acquire.duration` e a atividade `"RingBufferPlus.Acquire"` não distinguiam, no caminho de falha, um `AcquireTimeout` genuíno expirando de um cancelamento comum (pelo chamador ou por shutdown) via tag consultável — o status já corrigido no O2 carregava essa distinção, mas não havia tag equivalente para quem filtra/agrega por tag em vez de status. **Corrigido:** duas tags novas, `acquire.timed_out` e `acquire.cancelled`, adicionadas em `acquire.duration` e na atividade, no caminho de falha. **Nota de processo:** esta correção foi avaliada como de baixo risco/puramente aditiva e verificada só com um novo teste passando (verde) — sem o ciclo completo de reversão temporária para confirmar vermelho antes, um desvio deliberado do protocolo vermelho→verde padrão, registrado aqui em vez de silenciado. | `RingBufferManager.cs` (`AcquireCoreAsync`) | Tags `acquire.timed_out`/`acquire.cancelled` — implementado. Novo teste: `AcquireDuration_DistinguishesGenuineTimeoutFromCallerCancellation`. |
| **O7** — **novo, Rodada 5 (2026-08-21) — ✅ CORRIGIDO — escalado, você escolheu "Corrigir agora"** | **Média — confirmada em execução (protocolo vermelho→verde completo)** | No `finally` de `MoveToCapacityAsync`, o cálculo de `cancelledByShutdown` (`!ok && token.IsCancellationRequested`) podia mascarar uma falha genuína de factory ocorrida mais cedo no mesmo lote de scale-up, se um `DisposeAsync()` comum corresse depois e cancelasse uma tentativa *posterior* dentro do mesmo lote — a operação inteira era reportada como um cancelamento comum de shutdown (`ActivityStatusCode.Ok`, `cancelled=true`) em vez da falha genuína que de fato era, sem nenhum rastro de que aconteceu. **Escalado com duas opções (registrar para rodada futura vs. corrigir agora) — você escolheu corrigir agora.** Corrigido propagando um novo sinal `HadGenuineFailure` de `CreateItemsAsync` (assinatura mudou de `Task<int>` para `Task<(int Created, bool HadGenuineFailure)>`) até `MoveToCapacityAsync`, incorporado ao cálculo: `cancelledByShutdown = !ok && token.IsCancellationRequested && !hadGenuineFailure`. Confirmado **vermelho** (cálculo antigo restaurado temporariamente; a métrica/atividade reportou `cancelled=true`/status mascarado, exatamente o achado) → **verde**. | `RingBufferManager.cs` (`CreateItemsAsync`, `MoveToCapacityAsync`'s `finally`) | Sinal `HadGenuineFailure` propagado de `CreateItemsAsync` até o cálculo de `cancelledByShutdown` — implementado. Novo teste: `ScaleUp_WithAGenuineFailureThenRacedByDisposeAsync_StillReportsTheFailure_NotJustCancelled`. Ver [plano-de-acao.md](./plano-de-acao.md), Registro de execução, Rodada 5. |
| **O8** — **novo, Rodada 5 (2026-08-21) — ✅ CORRIGIDO** | **Baixa — mecânico (nível de log, sem mudança de comportamento)** | A mensagem de timeout do grace period introduzida pelo F16 (Rodada 4) — `"DisposeAsync did not wait for {N} orphaned heartbeat callback(s)..."` — era logada via `LogMessage` (nível informativo), o mesmo nível usado para as mensagens de desambiguação shutdown-vs-falha em outros lugares. Mas este caso é diferente em natureza: não é "shutdown vs. falha genuína", é "shutdown concluído, mas um recurso ainda não está confirmadamente disposed" — um vazamento real, de duração indeterminada, além do ponto em que `DisposeAsync()` já retornou, comparável em significância ao `LogWarning` já existente de "RingBuffer sem recurso". **Corrigido:** nível escalado de `LogMessage` para `LogWarning`. | `RingBufferManager.cs` (`DisposeAsync`) | Escalar de `LogMessage` para `LogWarning` — implementado. |
| **O9** — **novo, Rodada 5 (2026-08-21) — ✅ CORRIGIDO (doc, fundido com Usabilidade F-U38 — mesmo achado, mesma correção)** | **Baixa** | Ver F-U38. | `doc/guides/usage-observability.md` | Ver F-U38. |
| **O10** — **novo, Rodada 5 (2026-08-21) — ✅ CORRIGIDO (doc)** | **Baixa** | `usage-observability.md` não deixava claro que a tag `cancelled` significa algo diferente dependendo do sinal em que aparece: no Acquire (métrica/atividade), `cancelled=true` só quando o *próprio* token do chamador encerrou a chamada (um shutdown comum fica `false`, distinguível via `timed_out` — o que o O6 acrescentou); no Scale, `cancelled=true` quando um `DisposeAsync()` comum (o shutdown do próprio buffer, não um token do chamador) cancelou uma operação de escala em andamento. **Corrigido:** parágrafo esclarecedor adicionado, para quem agrega os dois sinais não assumir que significam a mesma coisa. | `doc/guides/usage-observability.md` | Parágrafo esclarecedor — implementado. |
| **O11** — **novo, Rodada 6 (2026-08-21) — ✅ CORRIGIDO (doc)** | **Baixa** | `usage-background-logger.md`'s "Trade-offs / limitations" dizia que uma mensagem enfileirada depois do canal de log completado é perdida, e chamava isso de "inherent to any bounded-lifetime background consumer" — texto que lê como inevitável independente de configuração. Reproduzido em execução que essa perda é específica do modo `BackgroundLogger(true)` (fila fire-and-forget): sob `BackgroundLogger(false)` (padrão, síncrono), a mesma mensagem tardia ainda chega ao `Logger`/`OnError` eventualmente (atrasada, não perdida), porque o caminho síncrono nunca depende do canal estar aberto. **Corrigido:** frase reescrita dizendo que a perda é específica do `BackgroundLogger(true)`, e que o modo síncrono ainda entrega a mesma mensagem tardia, só que atrasada. | `doc/guides/usage-background-logger.md:31` | Reescrever a frase — implementado. |

**Metodologia deste passe:** leitura completa de todo `LogError`/`LogMessage`/`LogWarning` em `RingBufferManager.cs` (confirmado, via grep, que não há chamadas de log em nenhum outro arquivo de produção exceto `RingBufferBuilder.cs`'s checagens síncronas de `ValidateBuild`, irrelevantes ao padrão shutdown-vs-falha), classificando cada um quanto a "pode um shutdown comum alcançar este ramo como algo diferente de uma falha genuína?". Do lado de métricas/traces, probes com `MeterListener`/`ActivityListener` reproduziram O1/O2 em execução real, não só por leitura de código.

---

## 9. Metodologia

Quatro passes de auditoria rodaram de forma independente e em paralelo a partir da Rodada 4 (três até a Rodada 3), cada um com seu próprio escopo e sem visibilidade sobre o trabalho ou as conclusões dos outros, seguindo os eixos combinados antes de começar:

1. **Estabilidade** — `Core/RingBufferManager.cs`, `ScaleSwitch.cs`, `Core/AutoScaleDecision.cs`, `Core/LogMessageBackground.cs`, testes de concorrência/escala, ADR001 (modelo de concorrência), ADR005 (dispose assíncrono).
2. **Resiliência** — as interfaces de builder de autoscale/elástico, `HostingExtensions.cs`, `RingBufferExtension.cs`, ADR003 (algoritmo de autoscale), ADR008 (observabilidade), os samples de RabbitMQ e trigger-scale.
3. **Usabilidade** — toda a superfície pública de builder/serviço, `RingBufferDefault.cs`, `RingBufferValue.cs`, README, os 10 guias, ADR004/ADR007, CHANGELOG, todos os 5 samples.
4. **Observabilidade** (a partir da Rodada 4) — todos os pontos de `Logger`/`OnError`/`Meter`/`ActivitySource` em `RingBufferManager.cs`, ADR008, `usage-observability.md`, `usage-background-logger.md`.

Achados marcados como **"confirmado em execução"** foram reproduzidos com probes de console descartáveis em um diretório temporário, cada um referenciando a biblioteca compilada via referência de projeto — **o repositório em si nunca foi modificado**. Achados marcados como **"suspeita, não reproduzida"** são conclusões só de leitura de código que o passe responsável não conseguiu ou não tentou disparar de forma determinística; estão sinalizados como riscos de higiene, não bugs demonstrados. Nenhum achado abaixo do status "suspeita" é afirmado como fato.

Onde dois passes descreveram a mesma região de código subjacente por ângulos diferentes (a causa raiz da §2; a região do contador de falhas do autoscale tocada por F7/R7/U-07; o `LockWhenScaling` tocado por F8/U-06/U-10), ambas as leituras foram mantidas e referenciadas cruzadamente em vez de fundidas em uma só, já que cada uma contribui com evidência que a outra não tinha.

---

## 10. Veredito final

**Atualização (2026-08-20):** o veredito original abaixo falava em "segurar o posicionamento de pronto para produção da v5.0.0" — linguagem que tratava a v5.0.0 como ainda maleável. Ela não é: já foi publicada (2026-08-12) e este relatório não pede para revisitá-la. O que segue é o registro histórico do que motivou o trabalho, mantido como está; a ação concreta hoje é: **não fechar a v5.1.0** até que P0 (✅ concluído), P1 (✅ concluído, exceto U-07, fechado junto com a Decisão C) e as decisões do P2 (A, B e C — ✅ todas decididas e implementadas) estejam resolvidos. Resta apenas o P3 (backlog de higiene, sem urgência de release).

O fato de maior prioridade que motivou tudo isso: uma factory que lança exceção — o modo de falha normal para exatamente os recursos que esta biblioteca existe para agrupar (conexões de banco de dados, canais RabbitMQ) — matava o engine permanentemente e podia travar todo método público assíncrono, incluindo o que deveria encerrá-lo de forma limpa. Isso reproduzia com uma linha de código de usuário comum, reproduzia em um buffer de capacidade fixa simples, e nada na suíte de testes na época o exercitava. **Corrigido em P0#1 (F1/R1).**

Os dois passes técnicos confirmaram, de forma independente, que a aposta arquitetural central do rewrite v5 — um único consumidor em canal que possui todo o estado de escala — de fato funciona: nenhuma atualização perdida, nenhum estado corrompido, nenhum deadlock na lógica de escala em si. O que faltava era um perímetro defensivo, hoje já construído (fronteira de exceção, guardas atômicas, timeout de heartbeat aplicado, limpeza incondicional do dispose) e verificado com testes de regressão.

**Sequência seguida:** P0 e todo o P1 implementados com protocolo vermelho/verde (ver [`plano-de-acao.md`](./plano-de-acao.md) para o registro completo). A Decisão A (§4.1) virou [ADR007 V02](../doc/adr/ADR007V02-redesign-of-the-public-fluent-api-surface.md) + [ADR004 V02](../doc/adr/ADR004V02-semantic-versioning-policy-and-fluent-api-stability.md) (exceção de versionamento). A Decisão B (§4.2, retry de `WarmupAsync`) virou [ADR011 V01](../doc/adr/ADR011V01-retry-path-for-a-failed-warmup-async-instead-of-a-permanently-broken-instance.md). A Decisão C (§4.3, limiar de falhas do autoscale) foi resolvida corrigindo o código (`>=`), fechando também U-07. As três decisões escaladas do P2 e todo o P3 (comportamento/código + lote de documentação) foram implementados e verificados (97/97 testes passando em net8.0/net9.0/net10.0), commitados e enviados (`526c8ba`).

**Rodada 2 (2026-08-20):** com o P3 concluído, uma nova auditoria completa (3 passes independentes, mesma metodologia) rodou contra o estado atual do código, focada em (a) confirmar que nada regrediu, (b) reavaliar o R13, e (c) procurar achados que só ficam visíveis depois que o perímetro defensivo do P0 já existe. **Nenhuma regressão encontrada** — todos os 47 achados fechados na Rodada 1 continuam corrigidos. **6 achados novos** (F12-F14, R14-R16) mais **5 de documentação** (U-25 a U-29), destacando-se o **F12 (Alta, confirmado em execução)**: o timeout do heartbeat agora dispara `Invalidate()` seguido de disposal síncrono do recurso pooled, enquanto o callback órfão (que não pode ser cancelado) pode ainda estar tocando nele — uma corrida de use-after-dispose real, só visível agora porque a correção do R3/P0#4 (Rodada 1) tornou esse caminho de timeout finalmente alcançável. E o **R14 (Média-Alta, confirmado)**: um único item de scale-up lento/falho aborta o lote inteiro, sem tentar os itens restantes mesmo com orçamento de sobra. Nenhum desses novos achados foi corrigido ainda — aguardando decisão sobre priorização/abordagem. Ver [`acompanhamento-convergencia.md`](./acompanhamento-convergencia.md) para o registro formal da rodada.

**Rodada 3 (2026-08-20):** nova auditoria completa (3 passes independentes) contra o estado pós-Rodada-2, com um objetivo adicional explícito: reconfirmar as próprias correções da Rodada 2 (F12, R14, R15, U-25 a U-29), não só procurar achados novos. **Nenhuma regressão** — todas as correções da Rodada 2 seguem corretas. Um achado importante confirma exatamente o padrão que esta auditoria recorrente existe para capturar: **F15 (Alta)**, uma lacuna residual na própria correção do F12 — o guard do catch de timeout do heartbeat não cobria o caso em que um `DisposeAsync()` comum (não um timeout de pulso) cancela o callback ainda em execução, reabrindo a mesma corrida de use-after-dispose que o F12 tinha corrigido. Achado de forma independente por dois passes diferentes (Estabilidade e Resiliência, como "Finding B"). O passe de Resiliência também encontrou um achado irmão em outro local (**R17**, mesma ambiguidade do R15 em `WarmupCoreAsync`) e reavaliou o **R16** (Rodada 2, "Baixa, não reproduzida") — reproduziu-o de fato através da própria interação com o R14 (uma factory com tolerância a falhas tende a pousar fora de tier com mais frequência), levando a uma correção arquitetural (não só uma extensão de elegibilidade) confirmada com o mantenedor antes de implementar: a margem de segurança do limiar de scale-down passou de um valor fixo (calculado uma vez a partir de Min/Init/MaxCapacity) para um valor escalado pela capacidade atual, garantindo que qualquer posição fora de tier eventualmente se torna elegível para scale-down. O R13 foi reavaliado e ganhou uma quantificação nova (amplificação linear exata com o R14 quando a falha tolerada é um timeout), endereçada só por documentação, decisão de arquitetura mantida. **Todos os achados acionáveis da Rodada 3 estão corrigidos**, com protocolo vermelho→verde onde havia comportamento (107/107 em net8.0/net9.0/net10.0): F15, R16, R17, a nota de documentação do R13×R14, e o lote de Usabilidade (F-U30 a F-U34) — decidido diretamente por severidade (F-U30, Média; F-U31/F-U32, Baixa-Média; F-U33/F-U34, Baixa), sem trade-off em nenhum dos cinco, mesmo padrão dos lotes U-25 a U-29/U-15 a U-29 já fechados sem escalonar. A varredura do F-U31 (links `ADR007V01`/`ADR001V01` obsoletos) também achou 2 referências extras em `doc/architecture/overview.md`, fora do escopo de busca original — corrigidas junto.

**Revisão adicional (2026-08-21), antes de encerrar a rodada:** uma consulta ao advisor sobre a própria correção do R16 achou três lacunas que o fechamento inicial deixou passar: (1) a doc XML de `AutoScaleAcquireFault` ainda descrevia as fórmulas fixas antigas (o próprio ADR003 a cita como fonte de verdade) — corrigida, e o `doc/architecture/overview.md` instrui explicitamente revisar/superseder o ADR relevante em vez de só corrigir o comentário, então **o mantenedor decidiu criar o [ADR003V02](../doc/adr/ADR003V02-median-sample-autoscaling-algorithm.md)** (emenda, não uma nova decisão — o algoritmo de mediana em si não mudou) registrando o refinamento; (2) o comentário de código que descrevia a correção como "sempre alcançável" estava incorreto num caso degenerado (capacidade inicial = 2, onde a margem se reduz à própria `currentCapacity` e a mediana nunca a supera) — confirmado como **pré-existente** (a fórmula fixa original tinha o mesmo problema), registrado como **R18** e não corrigido nesta rodada, por decisão do mantenedor; (3) o F-U33 estava sem o marcador ✅ CORRIGIDO no relatório, inconsistente com `acompanhamento-convergencia.md` — corrigido.

Resta F13/F14 (reavaliados, inalterados, ainda sem reprodução) e R18 (novo, registrado por decisão, sem ação planejada) — ver [`acompanhamento-convergencia.md`](./acompanhamento-convergencia.md).

**Rodada 4 (2026-08-21):** primeira rodada com 4 pilares independentes, Observabilidade rodando pela primeira vez como passe dedicado (não mais achados de raspão por outro pilar). **Nenhuma regressão** — F15, R16, R17 reconfirmados corretos (Resiliência inclusive rodou um sweep exaustivo determinístico das 1.573 combinações de `(min, capacity, max)` de `EvaluateScaleDown`, confirmando `capacity == 2` como o único valor degenerado do ramo superior). **6 achados novos corrigidos** com protocolo vermelho→verde: **F16** (Média-Alta) — `DisposeAsync()` podia retornar antes do dispose deferido do F12/F15 rodar de fato, um vazamento real se o processo saísse logo depois; **F17** (Média) — `ObjectDisposedException` não capturado no acquire interno do heartbeat durante shutdown, logado como erro; **R19** (Média-Alta) — espelho exato do R18 no ramo inferior (`minCapacity == 2` exigindo ociosidade total); **O1** (Média) e **O2** (Baixa-Média) — o padrão R15/F15/R17 replicado na camada de métricas/tracing (scale e acquire, respectivamente). O **R18**, que a Rodada 3 tinha registrado sem corrigir, foi reavaliado: como o R19 tornou os dois lados do mesmo defeito visíveis juntos, o mantenedor decidiu corrigir a causa raiz de vez (limiar limitado a `currentCapacity - 1` em ambos os ramos) em vez de manter os dois só registrados. Durante a implementação do teste do F17, uma **lacuna residual própria do F12/F15** foi achada e corrigida na mesma rodada (**F18**, Baixa-Média) — não por nenhum dos 4 agentes, mas ao verificar o mecanismo do próprio guard `!heartbeatWork.IsCompleted`. O lote de Usabilidade (**U-30 a U-36**, 7 achados — 1 Alta, 1 Média-Alta, 5 Baixa/Baixa-Média) e o de Observabilidade (**O1, O2, O4, O5**) foram todos corrigidos, exceto O4 (resolvido como efeito colateral da correção do R18, sem ação separada necessária). 116/116 testes passando em net8.0/net9.0/net10.0. O levantamento inicial desta rodada *teve* achados Média-Alta (F16, R19) — não Alto/Crítico — então esta é a primeira rodada real candidata a contar como "limpa" para o critério de convergência (zero Crítico/Alto no levantamento inicial). Resta apenas F13/F14 (reavaliados, inalterados) — ver [`acompanhamento-convergencia.md`](./acompanhamento-convergencia.md).

**Rodada 5 (2026-08-21):** quinta auditoria completa, quarta com os 4 pilares independentes, contra o estado pós-Rodada-4. **Esta rodada não é "limpa"** — o próprio levantamento inicial teve um achado Alta (**F19**, Estabilidade) — então o contador de rodadas limpas consecutivas reinicia a partir da rodada que vier depois desta (a Rodada 4, sozinha, não bastava para o critério de 2 consecutivas). **3 achados novos de Estabilidade, todos corrigidos** com protocolo vermelho→verde completo: **F19** (Alta) — o loop de drenagem de itens do `finally` de `DisposeAsync()` usava um `foreach` bruto em vez do helper `DisposeItemsDefensivelyAsync` já existente, abortando (e vazando tudo o resto, mais a própria instrumentação do buffer) no primeiro item cujo `Dispose()` lança — mesmo modo de falha da Rodada 1 (F5), gatilho diferente; **F20** (Média-Alta) — com `BackgroundLogger(true)`, `DisposeAsync()` completava `_logQueue`/aguardava `_loggerTask` antes de vários dos seus próprios passos geradores de log mais tarde no método, descartando-os silenciosamente; **F21** (Baixa-Média, escalado — você escolheu "corrigir agora") — `_pendingHeartbeatDisposals` (introduzido no F16) crescia sem limite sob um `HeartBeat` cronicamente mais lento que seu próprio orçamento de pulso, corrigido podando entradas já concluídas a cada novo `Add`. **2 achados de Resiliência, doc-only, corrigidos direto, sem trade-off:** **R20** — `usage-rabbitmq.md` ambíguo sobre `maxConsecutiveFactoryFailures` não ajudar `WarmupAsync`/`BuildWarmupAsync`; **R21** — `ADR003V02` ganhou um parêntese qualificando seu "geralmente" à luz do teto que a própria Rodada 4 introduziu no mesmo ADR. **2 achados de Usabilidade, doc-only, corrigidos direto:** **F-U37** — `usage-heartbeat.md`/`concepts.md`/`usage-dependency-injection.md` não mencionavam que `DisposeAsync()` pode esperar pelo disposal deferido de um callback de heartbeat órfão (F16); **F-U38** — a seção "Logging" de `usage-observability.md` não documentava a mensagem de timeout do grace period do F16 (fundido com **O9**, mesmo achado). **5 achados de Observabilidade:** **O6** (Baixa-Média, mecânico/aditivo — a única correção desta rodada sem o ciclo vermelho→verde completo, verificada só com um teste verde novo, desvio deliberado e registrado como tal) — novas tags `acquire.timed_out`/`acquire.cancelled` em `acquire.duration`/na atividade de Acquire; **O7** (Média, escalado — você escolheu "corrigir agora") — `cancelledByShutdown` em `MoveToCapacityAsync` podia mascarar uma falha genuína ocorrida mais cedo no mesmo lote de scale-up quando um shutdown comum cancelava uma tentativa posterior do mesmo lote, corrigido propagando um sinal `HadGenuineFailure` de `CreateItemsAsync`; **O8** (Baixa, mecânico) — a mensagem de timeout do grace period do F16 escalada de `LogMessage` para `LogWarning`, por ser um vazamento real de duração indeterminada, não uma ambiguidade shutdown-vs-falha comum; **O9** (Baixa, doc, fundido com F-U38); **O10** (Baixa, doc) — esclarecimento de que `cancelled` significa coisas diferentes em Acquire vs. Scale. 121/121 testes passando em net10.0 — as verificações intermediárias desta rodada usaram só net10.0 (convenção já estabelecida); os 3 TFMs ainda serão reconfirmados antes do commit/push. Restam apenas F13/F14 (registrados desde a Rodada 2, sem reavaliação específica reportada nesta rodada) — ver [`acompanhamento-convergencia.md`](./acompanhamento-convergencia.md).

**Limpeza de backlog entre rodadas (2026-08-21, depois da Rodada 5 já commitada — não é o início da Rodada 6):** com a Rodada 5 encerrada, o mantenedor pediu para revisar os 3 achados abertos remanescentes (F13, F14, R13) e decidir cada um antes de abrir a próxima rodada. **F13 removido:** o `catch` por-comando morto em `RunEngineAsync` foi deletado (dead-code removal pura, sem mudança de comportamento — suíte net10.0 121/121 antes e depois, sem protocolo vermelho→verde aplicável). **F14 fechado sem evidências:** 3000 iterações de probe e 2 rodadas de reavaliação sem uma única ocorrência — decisão de parar de carregá-lo como item aberto, sem mudança de código. **R13 fechado por decisão:** a 3ª confirmação da mesma decisão arquitetural (Rodadas 1 e 2 já a haviam confirmado) — deixa de ser lido como "parcialmente endereçado"/pendente; a mitigação por documentação já implementada permanece, a decisão de não mudar a arquitetura é final. Nenhum achado aberto resta no relatório após esta limpeza — ver [`acompanhamento-convergencia.md`](./acompanhamento-convergencia.md) para o registro formal.

**Varredura de código morto entre rodadas (2026-08-21, mesma sessão de limpeza):** a remoção do F13 levantou uma pergunta do mantenedor — já que um guardrail defensivo pôde ficar morto depois que uma correção mais específica foi empilhada por cima, faz sentido varrer o resto do código por outros guardrails na mesma situação? Concordamos em fazer a varredura e decidir 1 a 1. Resultado: **1 achado novo (F22, Baixa)** — o catch `OperationCanceledException` em torno de `Task.WhenAll(pending)` em `DisposeAsync()` tinha ficado redundante com os próprios catches externos de `RunEngineAsync`/`RunHeartbeatAsync`/`RunSampleTickAsync`, construídos incrementalmente rodada após rodada. Verificado por raciocínio estrutural (rastreando todo caminho de propagação possível, inclusive via `ErrorHandler` do usuário) e por um probe empírico (suíte completa rodou sem nunca atingi-lo antes de remover). **Removido, a seu pedido.** Nenhum outro candidato nos 16 arquivos de produção (2486 linhas) atingiu a mesma barra de alcançabilidade provada — os quase-candidatos descartados (o catch de `RunLoggerAsync`, a guarda de `AutoScaleDecision.Median`, o placeholder `RingBufferDefault.Capacity`) estão documentados na linha do F22 acima.

**Rodada 6 (2026-08-21):** sexta auditoria completa, quinta com os 4 pilares independentes, contra o estado pós-Rodada-5 mais as duas limpezas entre rodadas (commits `fc2388d`, `66d3b42`). **Esta rodada também não é "limpa"** — o levantamento inicial teve um achado **Alta** (**F23**, achado independentemente pela Estabilidade e pela Observabilidade — mesma causa raiz, mesma correção, fundido no relatório como fez o F-U38/O9 na Rodada 5) — então o contador de rodadas limpas consecutivas reinicia de novo a partir da rodada seguinte; a Rodada 4, isolada, nunca chegou a bastar para o critério de 2 seguidas. **F23 (Alta), corrigido com protocolo vermelho→verde completo:** nenhuma chamada a `Logger`/`ErrorHandler` estava protegida contra o próprio callback lançar exceção — sob `BackgroundLogger(true)`, isso matava o pump de log silenciosamente na primeira mensagem ruim, sem nenhuma mensagem seguinte jamais entregue; sob o modo síncrono padrão, especificamente no timeout do heartbeat, isso perdia permanentemente um slot do pool (com `CurrentCapacity` mentindo o valor antigo para sempre) e fazia o próprio `DisposeAsync()` lançar ao chamador — violando diretamente seu próprio comentário de design ("must never throw"). Corrigido com um novo helper `SafeInvokeSink`, envolvendo toda invocação de `Logger`/`ErrorHandler` em `LogMessage`/`LogWarning`/`LogError`/`RunLoggerAsync`. **R22 ("O7-residual", Média), corrigido com protocolo vermelho→verde completo:** a correção do O7 (Rodada 5) só cobria o caminho em que `CreateItemsAsync` *retorna* sua tupla — quando o lote faz zero progresso, o método em vez disso *lança*, reabrindo o mesmo mascaramento pelo caminho do throw; corrigido propagando `hadGenuineFailure` também por esse segundo caminho. **F-U39 (Média) e O11 (Baixa), doc-only, corrigidos direto, sem trade-off:** F-U39 — `usage-observability.md` afirmava que a mensagem de grace-period-timeout do F16 loga "no mesmo nível informativo" das outras, mas o próprio O8 (Rodada 5), no mesmo commit, já a tinha escalado para `LogWarning`; O11 — `usage-background-logger.md` descrevia a perda de mensagem tardia como inerente a qualquer consumidor de vida limitada, quando na verdade é específica do `BackgroundLogger(true)` (o modo síncrono ainda entrega, atrasado). **Uma investigação que não terminou em correção — registrada como nota de metodologia, não como achado numerado:** a Estabilidade também caracterizou como "inconsistente" o contrato de exceção de `WarmupAsync`/`SwitchToAsync` no caso zero-progresso vs. progresso-parcial; escalado, o mantenedor inicialmente escolheu "sempre empacotar em `InvalidOperationException`" — mas antes de implementar, dois testes já existentes e passando (`WarmupAsync_WhenFactoryThrows_PropagatesTheRealException_AndDoesNotHang`, `SwitchToAsync_WhenFactoryThrowsDuringScaleUp_PropagatesRealException_AndEngineSurvives`) revelaram que o comportamento atual é **deliberado**, estabelecido desde a Rodada 1/P0#1 — o mantenedor revisou a decisão para manter o código como está e só corrigir a frase do R20 em `usage-rabbitmq.md` que afirmava um tipo de exceção específico incondicionalmente (ver a nota completa no fim de §5). Nenhum teste novo para este item, já que nenhum comportamento mudou. 124/124 testes passando em net10.0, 0 warnings no build completo da solução. F13/F14 já haviam sido fechados na limpeza entre rodadas (antes desta rodada começar) — nenhum achado aberto resta após a Rodada 6, ver [`acompanhamento-convergencia.md`](./acompanhamento-convergencia.md).
